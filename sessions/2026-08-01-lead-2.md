# 2026-08-01 — lead, second entry

Branch `queue-follow-ups`. Direct human commission: wrap up for unattended
operation.

Completed the owns-grant ADR record — the acceptance was decided and the hook
installed (#36), but the Status flip sat uncommitted and the Decision review
empty. Queued M2-09 (gate-set assertion, closing the removed-gate blindness
from the PR #27 review; chained after M2-03, M2-04 rebased onto it) and M1-10
(abandoned-claims sweep verb for tasks.sh; wrapper wiring stays human-side).
Fixed M2-07's scope: the corpus home (scripts/tests/) and docs/layout.md are
now in its owns list, so its worker does not stop at the boundary.
