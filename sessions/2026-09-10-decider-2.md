# 2026-09-10 — decider, second sweep

- Merged #171, the first 2026-09-10 decider entry: checks green; the folded
  escalation's blob identical to the one on
  `escalated/M4-23-swept-20260910-144103`; the v2 record's diff confined to
  the two header flips and the Decision review; every merge and close it
  claims in the log.
- Rejected #172, the planner's ninth M4 batch (M4-25 to M4-27), on M4-25's
  sixth criterion: it joins the first facts case to "the submissions recording
  the repository already holds for CIK 0002003750" and pins
  `0001213900-26-088707` as a filing that history does not name, and the tree
  holds two such recordings that differ exactly there —
  `a-ticker-resolves-to-its-history` (41 filings, tenth absent, #90) and
  `a-filer-the-gate-admits` (42 filings, tenth present with `reportDate`
  2026-06-30, #98). As written the criterion is unsatisfiable or satisfied
  only by a worker picking the reading that lets the run continue. Every
  other check passed: premise against the milestones and the log, each task
  traced, ids fresh, depends_on resolving, owns able to hold each diff,
  overlaps ordered. Reason in the comment; `plan-20260911-005803` deleted.
- Nothing deferred, no ADR accepted. `gh` ran inside the sandbox throughout.
- Escalated refs on origin, two, both left alone:
  `escalated/M4-22-swept-20260910-031420` holds #167's rejected work and no
  escalation file, salvageable there; `escalated/M4-23-swept-20260910-144103`
  holds the deliberate stop #171 already folded onto main.
- Open escalations on main, two:
  - `2026-09-08-companyfacts-cik-as-a-number.md`: answered on record by the
    accepted `fetch-normalize-v2.md`; the task that publishes v2 deletes it.
    That task was in #172 and returns with the next plan. Names no task,
    parks nothing.
  - `2026-09-10-M4-23.md`: as the last sweep recommended, a human restates
    M4-23's acceptance with the re-derived counts — twelve concepts at the
    half-year, eleven agreeing; eight at the quarter, seven agreeing; four
    answering filings — deleting the file in the same change. Structural at
    most; a human's because it rewrites a task's acceptance. Parks M4-23.
- For a human, carried from earlier sweeps: how a partial supersession is
  recorded, and what the stage emits once wired. One new: the accepted v2
  record says "the submissions fixture" for CIK 0002003750 holds 41 rows and
  does not name the tenth filing, which is true of one of the two recordings
  in the tree. The decision turns on neither, so nothing to revisit; any
  task or reader leaning on that sentence should name the recording.
- Refill: requested, `planner/requested` at 8f2b209. The queue on origin
  holds two — M4-22, M4-23 — below the line; no `plan-` PR open after #172
  closed; no planner escalation on main; the ref did not exist.
