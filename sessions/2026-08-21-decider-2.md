# 2026-08-21 — decider, second sweep

- Merged #116: the first 2026-08-21 decider session entry. Checks green,
  within the decider's own write surface. No ADR acceptance rode along.
- Deferred #117 (M4-03, the fetch → normalize contract at v1) for a human:
  substance passes — transcription faithful to the accepted record, the
  `versions` sha verified against the committed bytes, boundary held, the
  layout collision settled without touching the gate, task retired, session
  entry present. Blocked solely on the human-approved label for the two
  `contracts/` writes; the protected-paths check is red for exactly that.
  Comment on the PR says what approval is needed; nothing else is
  outstanding.
- Nothing rejected.
- Escalated ref `escalated/queue`: the same crash record earlier sweeps
  noted — `tasks.sh available` failed at c436ee8, no stderr. Left alone;
  the run retries on its own.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batches merged in #108 and #115 are
  for the wrong milestone.
- Refill: not requested — the queue holds M4-03 (claimed, PR open), M4-04
  and M4-05.
