# 2026-08-13 — planner

Branch `plan-20260813-214524`. Queued M3-09 and M3-10, both against M3's first
criterion — given a ticker, the engine retrieves that company's filing history
from EDGAR. Five of M3's six criteria are delivered; that one is not. Every
`impl Transport` in the tree is in `tests/`, so the stage describes a retrieval
it cannot perform, exactly as `lib.rs` says.

M3-09 builds the transport in `crates/fetch/src/egress/`, the one directory the
egress gate lets a connection be opened in. M3-10 records a fixture through it,
which is the only way anyone watches it reach EDGAR, and moves the recording
instruction off curl and onto the engine.

The batch ends there. What a real transport meets — a 403 for a declaration
EDGAR rejects, a 429, a redirect it did or did not follow — is shaped by what
M3-09 lands and what M3-10 sees, and scoping it now would be guessing.

For the decider: M3's prose names "one user-keyed provider for market prices"
and `hosts.rs` anticipates it, but no "Done when" criterion asks for it. I
planned to the criteria and queued none, since a task deriving from prose alone
is invented scope. Whether prices close M3 is a premise a human owns.
