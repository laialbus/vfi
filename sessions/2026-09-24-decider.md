# 2026-09-24 — decider

- Merged #223, the 2026-09-23 decider entry.
- Merged #224 (plan, M4-44 to M4-48). The premise holds: M4 is active and none
  of its six golden fixtures is written. Every task traces to M4's golden-fixture
  criterion and to `golden-claims-and-baseline-re-recording.md` or
  `suspension-fixture-is-the-bank-fixture.md`. The ids are new, every
  depends_on resolves, and the owns lists overlap only where depends_on orders
  them. The golden and benchmark harnesses live in `crates/normalize/tests/`,
  and the gates find fixtures without naming them, so M4-44's owns list can
  hold its whole diff. The six bank exclusions match v2.
- Merged #225 (M4-43). Checks are green and it stays in `crates/normalize/`.
  The task file is retired. It pins 32 periods, 51 silence values and 576
  `Unknown`s. The only assertions that changed are the two wrong-shape
  `short_term_investments` rows the record supersedes.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override.
- Escalated refs: none on origin. Open escalations on main: none.
- Refill: not requested. Origin holds six tasks: M4-38 and M4-44 to M4-48.
- For the M4-44 review: until the harness has a claims check, its claims are
  re-derived by hand, as the golden-claims record says.
