# 2026-09-25 — M4-49

Branch `M4-49`. `tasks.sh available` now parks a task on an
`escalated/<id>-*` ref on origin as well as on an escalation file, as the
accepted `escalated-refs-park-their-task.md` rules. `parked` reads the branch
list `read_branches` already holds, so no second read of origin. Three corpus
cases added; `check`, `claim`, `release` and `sweep` unchanged.

For the decider. The rule is `escalated/<id>-*` as written, so the sweep's own
`escalated/<id>-swept-…` archives park their task too until the ref is
deleted. The record does not name archives; if they should not park, that is a
narrower pattern and a follow-up.
