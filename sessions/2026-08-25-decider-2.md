# 2026-08-25 — decider, second sweep

- Merged #121: the first 2026-08-25 decider entry, carrying the acceptance
  write for `docs/adr/canonical-concepts-open-questions.md`. Checks green,
  within the decider's own write surface.
- Not merged #122 (M4-05, the canonical concept vocabulary at v1): writes the
  protected path `contracts/`, no human-approved label, `protected-paths`
  check red. Boundary holds and the ordering it asked for (#121 first) is now
  satisfied; commented on the PR. Waits for the human's label.
- Nothing rejected, nothing else deferred.
- Escalated ref `escalated/queue`: the same crash record earlier sweeps noted —
  `tasks.sh available` failed at c436ee8, no stderr. Left alone; the run
  retries on its own.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std's fs, net,
  time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batches merged in #108 and #115 are for
  the wrong milestone.
- Refill: not requested — the queue holds M4-05, claimed, its PR open on the
  human's label. Blocked, not drained.
