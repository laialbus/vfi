# 2026-09-25 — lead

Direct human commission: resolve the escalations that need no owner decision.
Three refs, one wall, on M4-38; and the process gap the third run named.

- `tie-and-undated-cross-into-v2.md`: a Rule 3 tie among all-valued filings
  crosses as each tied filing's attempt under its accession, carrying what
  candidate choice declined inside it, beside the tie — which is what v2's
  `Unknown` line already says. An unreadable `filed` is a stop, not a state.
  No contract byte or contracts-crate type moves.
- `escalated-refs-park-their-task.md`: `available` parks a task on its
  `escalated/<id>-*` ref as well as on the file; the sweep deletes the ref
  when it folds the file. Queued as M4-49.
- M4-38's acceptance carries the ruled crossing, the ruled failure for an
  unreadable `filed`, and the counts the stopped runs re-derived (422 of 576).
- The two escalation files ride in #227, not yet on main; they are deleted in
  a follow-up once it merges, since a deletion based on the sweep's branch is
  lost when both squash. The three `escalated/M4-38-*` refs are the sweep's or
  the owner's to delete.
