# 2026-08-01 — M2-03

Branch `M2-03`. `scripts/gates.sh` gains the `purity` gate. The deny-list lives
in the runner as data, one line per package with the reach linking it would hand
analyze — network, filesystem, clock, randomness, environment, effectful
logging, and the syscall layer they all bottom out in. The gate reads
`vfi-analyze`'s whole resolved tree, so a denied package behind a crate the list
has never heard of is caught too; the proof injects a local package carrying a
denied name into a scratch copy, which keeps it offline.

`scripts/gates.sh` is protected, so the pull request needs the `human-approved`
label. One escalation filed: a dependency list cannot see `std`, so anchor 4 is
guarded against analyze acquiring the libraries and not against `std::fs` in its
own source.
