# 2026-09-26 — M4-50

Branch `M4-50`. `violate_benchmark` now sizes its slowdown off the benched
stage's committed baseline: one `vec![0u8; N]` per call, `N =
ceil(bytes / passes / 10)`, at least one, as the accepted
`benchmark-proof-scales-to-the-baseline.md` rules. A baseline missing either
number stops the proof (exit 3). `benchmarks/normalize/baseline` gains
`passes 512`, and the harness reads its pass count from there instead of a
constant. On the stub `N` is 3.
