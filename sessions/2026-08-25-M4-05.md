# 2026-08-25 — M4-05

Branch `M4-05`. Published the canonical concept vocabulary at v1 —
`contracts/canonical-concepts/v1.toml` and its `versions` record — transcribed
from `docs/adr/canonical-concepts.md` and the record that closed its open
questions. Twenty-eight concepts across five filer kinds, six applicability
clauses, three states. The contracts gate now reports two contracts checked
where it reported one.

The transcribed record is still `Proposed` on `main`; its acceptance rides the
decider's own open PR #121, which changes no reading. This PR is written against
the readings as accepted there, so #121 merges first or alongside it.
