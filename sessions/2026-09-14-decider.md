# 2026-09-14 — decider

- Merged #184, the second 2026-09-13 decider entry: checks green, a session
  entry alone, its claims matching main.
- Merged #185, M4-23, and accepted `docs/adr/which-filing-sets-the-value.md`
  as Structural: the two lines #178 was rejected for are right, every
  quotation from the accepted record matches it, and the fixture claims
  re-derive by direct reading (filings and fact counts at the half-year and
  the quarter, both EPS figures, the quarter's net income in all four filings,
  no shared `filed` date across 10, 14 and 21 filings, the dividend elements
  CIK 0001859199 reports, no fixture carrying any element the registry maps
  to the three silence-read concepts). Its Decision review, Status and Decided
  date ride in this PR; until it merges, the file on main trails by one run.
- Merged #186, M4-26: inside `crates/normalize/` plus the session entry and
  the task file; the cover-page entry admitted only at an instant every fact
  of the filing names as its report period end, compared as characters; the
  v1 refusal's constant gone; the renamed M4-19 case keeps its assertions; no
  contract, registry, gate or fixture moved. One choice made inside the crate,
  named here: a filing whose facts disagree on the field admits nothing.
- Merged #187, M4-27: inside `owns`, the stage untouched, the harness change a
  header only, the case discovered by directory. Negative `StockholdersEquity`
  re-derived at the four instants the header names, and at 2024-03-31 in one
  10-Q with `MembersEquity` carrying it in another.
- Nothing rejected, nothing deferred, nothing Constitutional seen. `gh` needed
  the sandbox override from its second call, as in earlier sweeps.
- Escalated refs on origin: none. Open escalations on main: none.
- Carried for a human. New: the accepted Rule 3 record, Structural, for the
  review the tier requires. M4-27's premise was wrong — every earlier facts
  case already carries negative equity under the same element, which the
  planner should know before M4-28 and M4-29 restate it. CIK 0001859199
  reports dividends only under elements the registry does not name, so its
  two dividend concepts can fall to the silence zero although the filer paid;
  a registry or vocabulary question beside M4-30. From earlier sweeps: the
  #181 ruling on v2's three descriptive lines, how a partial supersession is
  recorded, what the stage emits once wired, and `company_facts` fetching the
  history itself.
- Refill: requested, at `planner/requested` on 493ac66. The queue on origin
  holds three — M4-28, M4-29, M4-30 — no `plan-` PR is open, no planner
  escalation sits on main, and the ref did not exist.
