# 2026-09-13 — M4-25

Branch `M4-25`. `contracts/fetch-normalize/v2.toml` published with its
`versions` line, and `crates/contracts` states it. `company_facts` reads the
document's `cik` in either spelling and sends it across at ten digits. Once the
document is known to be this filer's, it asks for the history (every page) and
joins `report_period_end` by accession. The escalation on the number spelling
is deleted.

Each facts case holds the filer's submissions document: the tree's own for CIK
0002003750, copied byte for byte, and a live recording for CIK 0001859199. New
case `a-filer-whose-key-is-a-bare-number`, CIK 0001715819, recorded live.
Every `expected` was written by a throwaway outside the workspace. The restated
two differ from before only by the field on each fact line. The four checks on
CIK 0002003750 are pinned. Proved by hand: a reader refusing the number, and a
key crossing unpadded, each turn the new case red.

For the decider. The retrieval now costs the history's requests on top of the
facts document. A funnel step that already holds a history could hand it in
instead; that is the wiring task's call. Recordings ran with the sandbox off and
declared `VFI fixture recorder vfi-fixtures@example.invalid`, not the user's
address. The registry already reads `stand_in` and `exact` as the alignment
record asks, and was not edited.
