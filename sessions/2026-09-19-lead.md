# 2026-09-19 — lead

Direct human commission, run interactively: the owner delegated the rulings the
decider had carried for a human since 2026-09-14, all but the wrong-shape
silence zeros, which stay with the owner.

- Four records, each carrying the owner's review on merge:
  `silence-zero-supplied-once-per-period.md` (the same-day silent tie is the
  zero), `declared-cash-dividends-stand-in.md` (the registry names
  `DividendsCash`, `DividendsCommonStockCash` and `DividendsPreferredStock`;
  `Dividends` is excluded; three vocabulary changes recorded for v3),
  `suspension-fixture-is-the-bank-fixture.md` (keep CIK 0001778784; its emit
  claims re-derive under `bank`), and `golden-claims-and-baseline-re-recording.md`
  (a claims file beside a recorded `expected`; a stage that starts doing work
  re-records its baseline in the same diff, threshold untouched).
- A fifth, on a second commission the same evening:
  `silence-zero-only-at-the-concepts-own-shape.md` (the 45 wrong-shape zeros
  are `Unknown`; the Rule 3 tables' `short_term_investments` rows and the emit
  record's counts are superseded).
- Queued M4-41 (the `stands` fix, after M4-36), M4-42 (the registry entries,
  unordered) and M4-43 (the shape rule, after M4-41). M4-38 now depends on
  M4-43, and its pinned counts are the ruled ones. Numbered past the planner's
  same-day batch on `plan-20260919-202103`, whose M4-39 and M4-40 ask a worker
  to propose what `golden-claims-and-baseline-re-recording.md` now decides;
  both say a run stops where an accepted record already answers, so that plan
  should close unmerged once this lands.
- The Structural reviews of the Rule 3, silence and emit records: read whole
  and upheld, with the two amendments the new records make. Recorded in those
  records' Touches lines since accepted records are not edited.
- `docs/adr/README.md` gains the partial-supersessions table, which is where a
  ruling that lived only in #181 or #209 now stays visible. That closes the
  #181 and partial-supersession notes.
- Closed with no action: M4-27's premise, already corrected in the batches that
  followed. `company_facts` fetching the history itself: the task that composes
  the fetch calls end to end passes the history it already holds, and the
  planner names that when it queues the wiring; nothing moves until then.
- Left for the owner: merge this PR (the hook forbids a non-decider merge;
  #209 the decider merged meanwhile), and the `human-approved` label on
  M4-37's PR when it opens.
