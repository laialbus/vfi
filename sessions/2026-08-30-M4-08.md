# 2026-08-30 — M4-08

- Branch `M4-08`, PR: proposes `docs/adr/tag-concept-registry.md`, Status
  Proposed, Authority Structural. Nothing implemented.
- The registry is data under a new top-level `registry/` — one file per concept,
  one per filer — versioned by the sha256 of its own bytes rather than a `v<N>`
  sequence, reached through one narrow interface owned by `vfi-normalize`.
- Four rule forms, kind-scoped, identified by a derived id; overrides edit the
  eligible set per CIK and may assert a period-bounded value with a cited
  source. All twenty-eight published concepts are accounted for, with the
  plausible-but-wrong elements listed as exclusions.
- Candidate choice and the restatement ruleset are named and left alone; the
  cases the unordered entry set hands to the first are listed.
- Task file deleted. Gates green.
