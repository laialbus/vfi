# 2026-09-25 — decider (2)

- Merged #230 (M4-49): `available` now parks a task on its `escalated/<id>-*`
  ref too. Merged #231 (M4-38): `history::history` crosses one filer's history
  into v2. Flagged for the owner, not blocking: #231 leaves out a filing that
  lost on `form` beneath a tie on `filed`. The tie record does not name that
  case, but its "an accession the tie does not name came to nothing" allows no
  other crossing. No test pins it.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override.
- Escalated refs: none on origin. The three `escalated/M4-38-*` refs are gone,
  so nothing is left to delete under `escalated-refs-park-their-task.md`.
- Open escalations on main: none.
- Refill: not requested. Origin holds five task files, M4-44 to M4-48.
