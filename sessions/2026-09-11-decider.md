# 2026-09-11 — decider

- Merged #173, the second 2026-09-10 decider entry: checks green, a session
  entry alone, its claims matching main.
- Merged #174 (M4-22): checks green; one file under `registry/filers/`, the
  session entry and the task file's deletion. Every figure re-derived from the
  raw company-facts document — 121 element-value pairs, 157 period-and-filing
  checks, every offered identity closing, the declined one short by 286,173 as
  stated; both points of the #167 rejection answered. Detail in the comment.
- Merged #175, the planner's ninth M4 batch (M4-25 to M4-29): premise against
  the milestones, the log and the accepted records; each task traced; ids
  fresh; depends_on resolving; owns holding each diff; overlaps ordered.
  M4-25's sixth criterion now names the 41-row recording by path, and its
  claims re-derive, as do M4-26's five-and-five answering filings and cover
  counts. Two wordings flagged in the comment, neither a defect for a worker:
  M4-28 lists `PaymentsOfDividendsCommonStock` as reaching `dividends_paid`,
  which the committed registry does only as one operand of a sum; M4-29 counts
  five narrower clauses where the vocabulary has six. A human may tighten
  M4-28's second criterion before it is claimable; it sits behind M4-25 and
  M4-27.
- Merged #176, the owner's resolution of `escalations/2026-09-10-M4-23.md`:
  human-authored, no agent trailer, the escalation's deletion and the one
  M4-23 criterion it named, no protected path. Merged as transport, not
  accepted by the decider; the ruling takes the escalation's second sentence,
  as the last two sweeps recommended. M4-23 is claimable.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override from its second call, as in earlier sweeps.
- Escalated refs on origin, two, both left alone:
  `escalated/M4-22-swept-20260910-031420` holds #167's rejected work, now
  superseded by #174; `escalated/M4-23-swept-20260910-144103` carries the
  escalation #176 resolved, not re-folded, since folding it would re-park
  M4-23 against the ruling. A human may delete both.
- Open escalations on main, one: `2026-09-08-companyfacts-cik-as-a-number.md`,
  answered on record by the accepted `fetch-normalize-v2.md`; M4-25, now
  queued, deletes it. Names no task, parks nothing.
- Carried for a human from earlier sweeps, nothing new on either: how a
  partial supersession is recorded, and what the stage emits once wired. The
  v2 record's singular "the submissions fixture", noted last sweep, is now
  pinned by M4-25 to the 41-row recording.
- Refill: not requested. The queue on origin holds six — M4-23, M4-25 to
  M4-29 — above the line; no `plan-` PR is open and no planner escalation sits
  on main.
