# 2026-08-27 — M4-06

Branch `M4-06`. Created `crates/contracts/`, package `vfi-contracts`, holding
the fetch → normalize surface at v1 as Rust: `Filer` with its two fields and
its facts, `Fact` with its eight, and `Period` as an enum, which is what makes
a period one shape and never both and never neither. The crate is a leaf and
no stage depends on it yet.

The drift M4-03 left open is closed by the tests gate, not a new one. Two
macros emit each type's field and shape names off its own declaration, and the
crate's own test compares them with what `contracts/fetch-normalize/v1.toml`
states, printing both readings when they part. Proved by hand both ways round:
a field renamed, added and dropped in the type, and a name and a date count
changed on the published side in a scratch copy. The gate set is unchanged at
ten and nothing was weakened.

`scripts/gates.sh` gains two edges and nothing else — `vfi-fetch` and
`vfi-normalize` onto `vfi-contracts`, which is not a stage, so no edge between
stages is added. It is a protected path, so this merge needs the label.
