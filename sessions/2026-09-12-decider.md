# 2026-09-12 — decider

- Merged #177, the 2026-09-11 decider entry: checks green, a session entry
  alone, its claims matching main.
- Rejected #178 (M4-23): checks green, boundary held, Structural and within
  reach, and every fixture claim re-derived by running the landed procedure
  over both fixtures against the committed registry — fifteen concepts at the
  half-year and eleven at the quarter, both tables to the digit and the
  accession, the six tag-change periods and the 2026 reversal, the transition
  period naming the 10-K/A throughout, 74 registry elements at #137, #155 and
  now, no shared `filed` date in either fixture. Two statements do not
  re-derive, and an accepted record is never edited: Consequences says the
  fixture's quarter names three filings where the record's own Decision and
  the fixture say two, and Context credits M4-18 with `filings.rs`, which
  M4-19 landed in #170. Reasons in the comment; recoverable at
  refs/pull/178/head; branch M4-23 is sweep-exposed past the claim lifetime;
  the task file stays. The reading taken and the rule as stated stand up, so a
  resubmission needs those two lines right and nothing else.
- Nothing deferred, no ADR accepted, nothing Constitutional seen. `gh` needed
  the sandbox override from its second call, as in earlier sweeps; outside the
  sandbox `$TMPDIR` is another directory, so a comment file written inside it
  is named by its absolute path or the close lands without its reason.
- Escalated refs on origin, two, both left alone as the last two sweeps left
  them: `escalated/M4-22-swept-20260910-031420` holds #167's rejected work,
  superseded by #174; `escalated/M4-23-swept-20260910-144103` carries the
  escalation #176 resolved, not re-folded, since folding it would re-park
  M4-23 against the ruling. A human may delete both.
- Open escalations on main, one: `2026-09-08-companyfacts-cik-as-a-number.md`,
  answered on record by the accepted `fetch-normalize-v2.md`; M4-25, queued,
  deletes it. Names no task, parks nothing.
- Carried for a human. New: #178's record names, under Consequences, that a
  later thin filing's silence zero — `short_term_investments` and the two
  dividend concepts — displaces an earlier filing's read figure under Rule 3
  as accepted, and no fixture reaches it. The record is right not to patch it,
  since a clause for it would prefer an attempt by how it was set; it wants its
  own record beside the vocabulary's silence readings before the task that
  implements Rule 3 is queued. Structural. From earlier sweeps: how a partial
  supersession is recorded — M4-23's resubmission will be the first, and the
  template's Status line has no value for a record one rule of which is
  superseded — and what the stage emits once wired.
- Refill: not requested. The queue on origin holds six — M4-23, M4-25 to
  M4-29 — above the line; no `plan-` PR is open and no planner escalation sits
  on main.
