# 2026-09-22 — M4-41

Branch `M4-41`. `standing::stands` now asks the vocabulary for the period's
silence zero once, where no answering filing reached the concept (nor, for a
conditional reading, its partner), and returns it without passing anything
through Rule 3. Two same-day silent filings come to that zero instead of a tie.
Where no zero is supplied, each filing's own is withheld and Rule 3 runs over
the read and asserted values only, as before.

The zero now carries the registry version instead of a filing's attempt
(`settling::SetBy::Silence`), so the period's zero and a lone filing's are one
value; the withheld attempt moved beside the state, on `filings::Attempted`.
Five hand-written cases in `tests/standing.rs`. One existing assertion is
rewritten to read the withheld attempt off the `Unknown` rather than off the
value it left.

Re-derived, not pinned: over all six merged fetch fixtures, every period and
every concept comes to the same state as on `d05fc0c`, with the silence values
differing only in that carriage. No fixture holds the same-day silent case.
