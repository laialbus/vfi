# 2026-09-02 — M4-10

- Branch `M4-10`, PR: gives the `registry` gate the per-filer half —
  `registry/filers/<cik>.toml`, its `cik`, `kind`, `include`, `exclude` and
  `assert` — and the refusals over it. Twenty-seven in-band cases, all planted.
  No filer file is written: the shape and the refusals land before the first
  override needs them.
- The general half is untouched and its twenty-two cases still run. What an
  entry is, is now read in one place, so an include and a general entry cannot
  drift; a problem names the filer file where it is one and the concept where it
  is the other.
- An assertion states an instant or a duration and never every period, and
  carries the accession and line it is read from. Nothing checks that citation.
- The cycle walk runs once more per filer, over the general edges less what its
  file excludes plus the `difference` rules it includes, so a filer's own rule
  cannot close a cycle the gate does not see. Two cases prove it, one caught and
  one clean. The run filed this as an escalation; the human decided the check is
  the gate's, and the file is deleted with the fix. M4-11 inherits the run-time
  half through "an override the gate would refuse fails to load".
- Task file deleted. Gates green.
