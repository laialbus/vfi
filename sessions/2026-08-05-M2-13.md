# 2026-08-05 — M2-13

Branch `M2-13`. `scripts/tasks.sh available` now withholds a task an open
escalation names, the way an unmerged dependency withholds it. A file is read
as naming a task when its name is `<date>-<task-id>.md` and the id is one the
queue holds, so a subject slug parks nothing; presence parks and deletion
restores, with no status written anywhere. Claiming, claim verification, and
the sweep are untouched. `escalations/README.md` states the rule; four corpus
cases pin it.
