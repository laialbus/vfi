# 2026-09-03 — planner

Queued M4-13 through M4-15, the fifth M4 batch, on the decider's refill request.
M4 stays active: both halves of the registry are data on disk, but nothing maps,
nothing resolves, and the ruleset that reconciles filings is unwritten.

M4-13 lands `reading` and `answers` on the registry, its gate and its reader —
the fields `docs/adr/candidate-choice.md` names "the task that writes the
fields", serving the criterion asking that choosing among candidates in one
filing follow a stated rule. M4-14 proposes the alignment ruleset, the criterion
asking that amendments, restatements and periods follow one, stated separately
from the mapping. M4-15 compiles the published concept vocabulary into
`vfi-contracts`, which holds one module for two published contracts today; it
serves the criterion asking each concept to resolve to one of three states, two
of which nothing in the engine can yet construct.

Ends before resolution and before the six named fixtures. The procedure's inputs
are three shapes not yet landed, and which filer shows a restatement is a choice
against a rule M4-14 has not stated.

For the decider: M4-13 and M4-15 are exclusive, so the batch runs one at a time
once either is claimed; M4-13 owns `scripts/gates.sh` and needs the
human-approved label at merge. M4-13 depends on M4-11 because it edits the
reader M4-11 writes.
