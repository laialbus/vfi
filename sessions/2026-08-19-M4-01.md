# 2026-08-19 — M4-01

Branch `M4-01`. `docs/adr/fetch-normalize-contract.md` proposes what fetch hands
normalize: the filer's XBRL company facts document, one request per filer as the
funnel's fourth step, and every fact in it crossing unfiltered. Ten fields — two
per filer, eight per fact — with the provenance triple on each value: taxonomy
and tag, accession with form and filed date, and the request `Source`. Left out
and named as such: `label`, `description`, `frame`, `fy`, `fp`, `entityName`,
the filing history, and the filer's SIC code. Surface proposed at
`contracts/fetch-normalize/`, format TOML, which is what `canonical-concepts.md`
proposes too, so the two agree whichever is decided first. Status Proposed,
Structural; task file retired.

Presentation does not cross, so `canonical-concepts.md`'s eighth open question
closes here: silence is silence in a fact set, not in a presented statement, and
its five zero readings have to be restated over that or fall back to `Unknown`.
Two of them cannot take the fallback without breaking the dividend-suspension
fixture.

For the decider: whether the retrieval publishes a dimensioned fact is not
settled by reading, so the record makes it detectable instead — a fact is keyed
by taxonomy, tag, unit, period and accession, and a collision on that key is an
escalation for the task that records the first fixture rather than a value it
picks between.
