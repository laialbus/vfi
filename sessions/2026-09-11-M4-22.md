# 2026-09-11 — M4-22

Branch `M4-22`. `registry/filers/` gains `0001859199.toml` — reAlpha Tech Corp.,
the filer `fixtures/fetch/a-filer-that-changed-its-fiscal-year` is recorded
from — asserting `kind = "operating"` and nothing else: no `include`, no
`exclude`, no `assert`. The argument sits in a comment beside the assertion.

Redelivers rejected #167. The investment-company refusal now places each
investment and investment result the facts carry, rather than claiming there is
one. The balance-sheet paragraph names only identities that close, and says the
2024-12-31 liabilities side is not one of them. Both sides of the fiscal-year
change read `operating`. No concept file changed, and the first filer's file is
untouched.
