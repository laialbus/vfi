# 2026-09-23 — M4-43

Branch `M4-43`. The vocabulary's silence zero is now supplied only at a period
of the concept's own shape, as the accepted
`silence-zero-only-at-the-concepts-own-shape.md` rules. One condition added in
`settling::supplies`, which is where the three readings already branch, so the
per-filing step and `standing::stands` reach it together; `supplied` takes the
period to ask it with.

For the decider. Both superseded counts re-derive on the merged fixture: 32
periods, 51 silence values each at a period of the concept's own shape, 576
`Unknown`s. Two existing results move, both wrong-shape zeros the record judges
unsupported: `short_term_investments` at the Rule 3 record's half-year and
quarter, now `Unknown` carrying every answering filing's attempt, and the
per-concept silence sweep, which now asks each concept at both shapes.
