# 2026-08-01 — lead

Branch `hook-owns-draft`. Direct human commission, no task file.

Drafted the owns-grant revision of the pre-edit guard
(docs/adr/protect-paths-owns-grant.md, proposed in #32): the installed floor
plus one loosening — a protected path the claimed task owns per its file on
origin/main, within a fixed grantable sublist, may be written; every failure
is the empty grant. Corpus extended with sixteen grant cases on a real git
fixture, plus a loud error when the candidate it judges is absent — a missing
draft used to read as 93 phantom denials. Verified: 109 cases, 0 failing,
identical under 3.9.6 and 3.14.2; floor sweep 3024 commands, 0 breaches;
0 bypasses allowed. Nothing installed; both drafts wait on the ADR's
acceptance.
