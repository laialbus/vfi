# 2026-09-23 — planner

Queued M4-44 to M4-48, the fifteenth M4 batch, on the decider's refill
request. M4-38 and M4-43 were already on origin. M4 stays active because none
of the six golden fixtures it names is written.

M4-44 makes `normalize()` the stage. It records the restatement filer as the
first fixture, with claims and `expected`, and re-records the benchmark in the
same diff, as `golden-claims-and-baseline-re-recording.md` rules. It is
exclusive and depends on M4-38. M4-45 to M4-48 each record one filer's fixture
after M4-44: the fiscal-year and tag change, the dividend suspension, negative
equity, and the bank with no gross profit. Each owns only its own directory.

Ends before the harness's claims check. That check needs its own record, and
the form it would check is the claims form M4-44 has not fixed yet.
