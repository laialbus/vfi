# 2026-09-17 — M4-29

Branch `M4-29`. New case `a-filer-whose-shape-has-no-gross-profit`: CIK
0002033615 (Magnolia Bancorp), 2591 facts, key spelled as the ten-digit string,
recorded live through `crates/fetch/src/bin/record.rs`. Its document carries no
element named `GrossProfit`, `CostOfRevenue`, `CostOfGoodsAndServicesSold`,
`CostOfGoodsSold` or `CostOfServices`, under any of its three taxonomies, so
none of the eight filings its facts came from states a fact under one. It is a
bank on the vocabulary's clause; the header gives the balance sheets and the
income statement runs that is read off, what the `operating` clause has no place
for, and where the other narrow-clause concepts stand. `expected` was written by
a throwaway outside the workspace, which reproduced all five existing facts
cases' `expected` byte for byte. Proved by hand: dropping one `Deposits` fact
from `expected` turns the case red. Two recordings taken apart were
byte-identical, and the recording holds no collision.

For whoever asserts the kind. No file is added under the filer registry here.
Both year-end balance sheets close to the dollar with no current section
subtotalled. The 2024 income statement does not close at the tax step in the
filing that states it — `IncomeTaxExpenseBenefit` -26,977 against a
`NetIncomeLoss` of -100,000 — because that is the one figure the 10-K filed
2025-03-28 does not round to thousands; the 10-K filed 2026-03-16 restates it as
-27,000 and closes.

For the decider. The task body says five concepts carry a clause narrower than
all five kinds. The published vocabulary at v1 has six: `gross_profit`,
`current_assets`, `current_liabilities`, `inventory`, `short_term_investments`
and `operating_income`. The header names all six. Nothing turned on the count.

The search ran on the XBRL frames API, then one company facts and one
submissions request per candidate. The recordings ran with the sandbox off,
because the stage's transport does not use the sandbox proxy, and declared
`VFI fixture recorder vfi-fixtures@example.invalid`. None of the search is
committed.
