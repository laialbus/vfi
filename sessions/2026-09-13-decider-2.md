# 2026-09-13 — decider, second sweep

- Merged #182, the first 2026-09-13 decider entry: checks green, a session
  entry alone, its three merge claims matching main.
- Merged #183, M4-25: checks green on the head after the human-applied
  `human-approved` label, every file inside `owns` plus the session entry and
  the task file, the escalation on the number-spelled key deleted in the same
  diff. Re-derived: `v2.toml` is v1's bytes with the two fields the accepted
  record states and the three descriptive lines the #181 ruling rewords, and
  nothing else; `versions` carries v2's sha256 as computed; `v1.toml` untouched;
  the reader refuses every malformed key by hand-written case and asks no
  history for a refused one; the two restated `expected` files are byte-identical
  to the old ones once the new field is stripped; the submissions document for
  CIK 0002003750 is the tree's own recording, byte for byte; the nine shared
  dates run 2024-03-31 to 2026-03-31 in filed order; the 193 facts of
  `0001213900-26-088707` alone carry the field empty; 20 of 41 history rows are
  empty and none carries a fact; the new case opens `filer 0001715819` over a
  document spelling `"cik":1715819`. No gate, lockfile, registry or normalize
  fixture moved.
- Nothing rejected, nothing deferred, no ADR accepted, nothing Constitutional
  seen.
- Escalated refs on origin: none; origin holds `main` alone.
- Open escalations on main: none. M4-25 deleted the last one, as the accepted
  `fetch-normalize-v2.md` directed.
- Carried for a human, Structural. From the last sweep: the #181 ruling that
  v2 rewords three descriptive lines the accepted v2 record says cross
  unchanged now lives only in #181 and in the published `v2.toml`; a line in
  that record's Decision review would keep it visible. New, from M4-25's own
  note: `company_facts` now fetches the filer's history itself, so a facts
  retrieval costs the history's requests on top of the document; the funnel's
  history step already holds that history, and whether the wiring task passes
  it in is that task's call, worth naming when it is queued. From earlier
  sweeps: how a partial supersession is recorded, and what the stage emits once
  wired.
- Refill: not requested. The queue on origin holds six — M4-23, M4-26 to
  M4-30 — above the line; no `plan-` PR is open, no planner escalation sits on
  main, and no request ref exists.
