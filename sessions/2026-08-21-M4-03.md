# 2026-08-21 — M4-03

Branch `M4-03`. Published the fetch → normalize contract at v1 —
`contracts/fetch-normalize/v1.toml` and its `versions` record — transcribed from
`docs/adr/fetch-normalize-contract.md`. The contracts gate now reports one
contract checked where it reported an empty set.

Settled the collision over what a directory under `contracts/` is: the layout
gives, not the gate. `contracts/` holds contracts and nothing else; the Rust type
the stages compile against goes to `crates/contracts/`, package `vfi-contracts`,
and is not created here. Written down in `docs/layout.md`. `scripts/gates.sh` is
untouched, so no check was traded and the merge waits on no signature the surface
itself does not already need.

Still unchecked, and named in the record as gaps: the drift check between the
published bytes and that type, and the golden fixture that proves fetch filters
nothing and that no two facts collide on their five identifying fields. Both are
later tasks; neither is a gap in this diff.
