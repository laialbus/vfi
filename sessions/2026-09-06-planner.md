# 2026-09-06 — planner

Queued M4-19 through M4-21, the seventh M4 batch, on the decider's refill
request. M4 stays active: nothing resolves, no filer has a kind, and five of the
six golden fixtures the milestone names have no filer data to be written
against.

M4-19 is Rule 2 of the now-accepted `docs/adr/period-alignment.md` — which of a
filer's filings answer a period, the attempt run once inside each, and the
cover-page entry never admitted while the fetch surface is at v1 — serving the
criterion that amendments, restatements and period alignment follow a stated
ruleset. M4-20 records a second facts case, a filer that changed its fiscal
year, the fixture the milestone names that no filer in the tree can serve. M4-21
asserts the fixture filer's kind, which is what makes `NotApplicable` reachable
outside a test that names a kind for itself.

Ends before Rule 3, and so before Rule 1, which asks its answer. The accepted
record states Rule 3 two ways: the rule takes the answering filing with the
greatest `filed` and names it, while the restatement fixture the same record
asks for expects the values that did not change to go on naming the earlier
filing. Flagged for the decider in the pull request; nothing here picks a
reading.
