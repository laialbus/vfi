# 2026-09-10 — decider

- Merged #166, the 2026-09-09 decider entry: checks green, a session entry
  alone, its claims matching main.
- Merged #169 (M4-24): checks green; one record under `docs/adr/`, the session
  entry and the task file's deletion. `fetch-normalize-v2.md` accepted,
  Structural, flagged for human review; its Decision review, Status and Decided
  date ride in this PR. Every repository and fixture claim re-derived, listed
  in the acceptance comment; two loose wordings noted there, neither a defect.
- Merged #170 (M4-19): checks green; diff inside `crates/normalize/` plus the
  session entry and the task file's deletion; lockfile, `normalize`, golden
  fixture, contract surfaces, registry data and gates unmoved. The M4-17 and
  M4-18 test edits are call-site rewrites through a wrapper passing every
  entry, no assertion changed. Rule 2 as accepted, the cover-page entry refused
  at v1 by the caller's admission and declining under an existing reason;
  fixture counts match the accepted record and are pinned green.
- Rejected #167 (M4-22): the kind is very likely right and every cited value
  exists in the fixture, but two claims in the argument the task makes the
  deliverable do not re-derive — "its one investment is the 115,000" against
  `OtherLongTermInvestments`, `Investments` and a 5,502,774 gain on sale of
  investments over the transition period, and a 2024-12-31 liabilities list
  short 286,173 of `Liabilities` while the PR says the identities close.
  Reasons in the comment; recoverable at refs/pull/167/head; branch M4-22 is
  sweep-exposed past the claim lifetime; the task file stays.
- Closed #168 (M4-23) without merge: an escalation alone, retiring nothing;
  folded here as the escalation sweep directs.
- Nothing deferred, nothing Constitutional seen. `gh` needed the sandbox
  override from its second call, as in earlier sweeps.
- Escalated refs on origin: none under `escalated/`. One deliberate stop on
  branch `M4-23`, folded byte-for-byte into this PR as
  `escalations/2026-09-10-M4-23.md`. Re-derived before folding: thirteen mapped
  elements in common at the half-year, twelve agreeing; four filings answering
  the quarter. The accepted alignment record's ten and nine are an undercount,
  explained neither by registry growth — the element set was byte-identical
  across its proposal and acceptance — nor by any reading of "mapped".
- Open escalations on main, two once this merges:
  - `2026-09-08-companyfacts-cik-as-a-number.md`: answered on record by
    `fetch-normalize-v2.md`, accepted this run; the task that publishes v2 and
    teaches the retrieval both spellings deletes it. Nothing for a human. Names
    no task, parks nothing.
  - `2026-09-10-M4-23.md`: recommend a human restates M4-23's acceptance with
    the re-derived counts — twelve concepts at the half-year, eleven agreeing;
    eight at the quarter, seven agreeing; four answering filings — deleting the
    file in the same change, and lets the superseding record say the accepted
    record's ten and nine were an undercount. Structural at most: it corrects a
    fixture claim inside an accepted Structural record through the supersession
    that record already needs. A human's because it rewrites a task's
    acceptance, which no worker may do, and the count sits in a record no one
    edits.
- Carried for a human from the 2026-09-09 entry, nothing new on either: how a
  partial supersession is recorded, and what the stage emits once wired.
- Refill: requested, `planner/requested` at d640b99. The queue on origin holds
  two — M4-22, M4-23 — below the line; no `plan-` PR is open, no planner
  escalation sits on main, and the ref did not exist.
