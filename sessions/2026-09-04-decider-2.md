# 2026-09-04 — decider, second sweep

- Merged #147, the first 2026-09-04 decider entry: checks green, a session
  entry alone, its claims matching main.
- Merged #148 (M4-15): checks green, diff inside the owns list plus the
  session entry and the task file's deletion; no protected path touched, so
  no label needed; the task was exclusive and no other branch was in flight.
  The type matches the published v1 bytes on every axis read against the
  file — three states, five kinds, twenty-eight concepts, each concept's five
  readings — and the comparison rides the tests gate. The two readings the
  worker flagged stand: `Value` carrying its amount as published text
  follows the fetch surface's precedent; `Silence::Conditional` leaving the
  two conditions as published prose adds no unchecked transcription. The
  TOML reader moved to the crate root so both modules read through one.
- Nothing rejected, nothing deferred.
- No escalated refs on origin; nothing to fold.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std.
  Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Refill: requested, `planner/requested` at 87c31ee. The queue holds three —
  M4-11, M4-13, M4-14 — which is at the line; no `plan-` PR is open and no
  planner escalation sits on main.
