# 2026-09-19 — planner

Queued M4-39 and M4-40, the fourteenth M4 batch, on the decider's refill
request; M4-36, M4-37 and M4-38 were left on origin. M4 stays active: the six
golden fixtures the milestone names are unwritten and nothing runs through the
stage's entry point.

Both tasks deliver an ADR proposal and no code, serving the golden-fixtures
criterion by answering what stands between the wiring and the fixtures. M4-39
asks what writes a fixture's `expected` when the harness refuses the subject
and there is no recording to read. M4-40 asks when a committed benchmark
baseline and its workload may be re-recorded, which the stage's entry point
forces the moment it stops copying its input. Both questions are the 2026-09-18
planner entry's, unanswered since; neither depends on the other or on the three
tasks already queued.

Ends there. The harness, the entry point and all six fixtures wait on these two
being accepted, and their shape is what the records decide. The
dividend-suspension claims and the same-day silent tie still wait on a human.
