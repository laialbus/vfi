# 2026-08-01 — M1-08

Branch `M1-08`. Drained the three installed proposals — the hook draft, the
lead prompt, and the frictions doc. The README table now lists only what is
left: the two launchd plists and the corpus test.

The pre-delete check that the hook draft matched its installed copy turned up
an escalation. They are byte-identical, but the installed copy crashes on
import under the `python3` that `.claude/settings.json` invokes and exits 1, so
it has refused nothing since PR #20. Its logic is green — 93/93 under 3.12.
