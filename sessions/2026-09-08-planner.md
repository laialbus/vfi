# 2026-09-08 — planner

Queued M4-22 through M4-24, the eighth M4 batch, on the decider's refill
request. M4 stays active: nothing resolves through the stage, Rule 3 of the
accepted alignment record is stated two ways, and the golden fixtures the
milestone names have no settled rule to be chosen against.

M4-22 asserts the kind of the second facts-fixture filer, the one that changed
its fiscal year, as M4-21 did for the first — serving the three-states criterion
and the fiscal-year-change fixture, whose period set Rule 1 makes a function of
the kind. M4-23 proposes the record superseding Rule 3 alone, as the decider
recommended, serving the criterion that amendments, restatements and period
alignment follow a stated ruleset. M4-24 proposes the fetch → normalize v2 that
fixes how a filer's CIK crosses, with `report_period_end` in the same version,
on the open escalation and the decider's recommendation; it serves the criterion
that filings are mapped into the canonical set, which today holds for one filer
in twenty.

Ends before Rule 3 in code, Rule 1, the v2 surface and any further recording,
each of which waits on one of the two records being accepted. All three are
claimable now beside M4-18; none touches `crates/normalize/`.
