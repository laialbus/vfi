# 2026-09-07 — decider

- Merged #156, the 2026-09-06 decider entry: checks green, a session entry
  alone, its claims matching main; the refill it requested was consumed and
  came back as #157.
- Merged #157, the planner's seventh M4 batch (M4-19 to M4-21): premise checked
  against the milestones and the merge history — M4-05, M4-09 to M4-13 and
  M4-15 landed, both rulesets accepted; every task traced to a criterion or to
  the accepted alignment record; ids fresh; depends_on resolving, M4-19 behind
  M4-18 and the other two free; owns lists able to hold their diffs — M4-20's
  case directory and the harness header both under its two paths, M4-21's one
  file a shape the registry gate already accepts, with no normalize test or
  benchmark pinning the filers half empty; overlaps ordered along the normalize
  chain. Touches only `tasks/` and `sessions/`.
- Merged #158 (M4-16): checks green; diff inside `crates/normalize/` plus the
  session entry and the task file's deletion; no protected path, no gate
  touched, the lockfile unmoved. Re-read against the crate the stages compile
  against: `Excluded` has private fields and `Applicability::excluding` is its
  only constructor, so the module's one call is the one path to
  `NotApplicable`; the tests read expected answers off `admits`, that same
  crate's own reading. The two shapes the worker flagged stand.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override after its first call, as before.
- One escalated ref on origin, `escalated/M4-13-20260906-064243`: the wrapper's
  crash record, unchanged since the last sweep. Left alone; the task landed as
  #155.
- Open escalations on main: none.
- For a human, carried from the planner's body and confirmed against the
  record: the accepted `docs/adr/period-alignment.md` states Rule 3 two ways.
  Its Decision names the greatest-`filed` filing for every agreeing value; its
  restatement fixture expects agreeing values to keep naming the earlier one.
  Both cannot hold on the merged fixture. Recommend the next plan queue a task
  proposing a superseding record for Rule 3 alone — Structural, so the decider
  can accept it. Nothing queued depends on it yet.
- Refill: not requested. The queue holds five — M4-17 to M4-21 — which is
  above the line.
