# 2026-07-30 — lead

Branch `m2-queue`. Direct human commission: queue the wrap-up work and M2.

Twelve tasks queued. M1-06–M1-09 wrap up M1: tasks.sh fails closed on
frontmatter key variants, claims verify themselves after the push (both per
decider-6's non-blocking flags on PR #18), the installed proposals drain, and
the stale worktrees sweep. M2-01–M2-08 build the floor from GOALS.md: the gate
runner wired into CI, then one gate per task — dependency direction, purity
deny-list, contract compatibility, fixture harness, benchmark harness, the
scripts gate — each exclusive and chained, since each extends the protected
runner; last, the slow-job responsiveness proof. `tasks.sh available` reads
the queue clean and offers M1-06, M1-08, M1-09, M2-01.
