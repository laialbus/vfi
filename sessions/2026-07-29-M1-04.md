# 2026-07-29 — M1-04

Branch: `M1-04`

Added `scripts/tasks.sh`, `scripts/ci-status.sh`, and `scripts/escalate.sh`,
the three interfaces `run.sh` already calls. `tasks.sh available` derives the
queue from `tasks/` frontmatter plus `git ls-remote`, treating a missing task
file as a merged dependency — the self-draining queue is the only status
record, so no list is kept here. Escalation filenames use UTC, matching the
existing records.
