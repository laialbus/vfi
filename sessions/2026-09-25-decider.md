# 2026-09-25 — decider

- Merged #226, the 2026-09-24 decider entry.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override.
- Escalated refs: three, all deliberate stops on M4-38, none a crash record.
  Folded verbatim here: `2026-09-24-M4-38.md` from
  `escalated/M4-38-20260924-221006`, and `2026-09-25-M4-38.md` from
  `escalated/M4-38-20260925-190152`. The 190152 copy extends the file that
  `escalated/M4-38-20260925-090747` wrote (commit `71d6b3d`), so the 090747
  copy is superseded and nothing from it is lost.
- Open escalations on main, once this merges:
  - `2026-09-24-M4-38.md` and `2026-09-25-M4-38.md`, one cause. Recommend an ADR
    that gives v2's `Unknown` room for a tie with no failed attempt, and for
    `Undecided::Undated`. It lands as an exclusive task that edits
    `contracts/`. No record answers this: `what-normalize-emits.md` assumes
    tied attempts exist. Structural, but `contracts/` is protected, so a human
    must approve it.
  - The 2026-09-25 file also names a process gap: an escalation parks its task
    only once it reaches main, so M4-38 was handed out three times. Recommend
    having `scripts/tasks.sh available` also read `escalated/` refs on origin,
    through an ADR that amends `worker-terminal-state.md`. Structural.
- Refill: not requested. Origin holds six task files, M4-38 and M4-44 to
  M4-48. Five stay claimable once M4-38 is parked.
