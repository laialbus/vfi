# 2026-09-13 — M4-23

Branch `M4-23`. Resubmits the record #178 proposed,
`docs/adr/which-filing-sets-the-value.md`: among the filings answering a period,
the one filed last sets the value, agreeing or not. The two lines the decider
rejected are corrected: the quarter's row names two filings, and M4-19 landed
`filings.rs`.

Brought up to main since #178. The boundary is read at `fetch-normalize` v2.
The own-period preference that `report_period_end` now makes stateable is argued
as an alternative, and it falls to both restatement fixtures. The claims about
what no fixture holds now cover CIK 0001715819. The silence-zero paragraph
points to M4-30, and its "no filer pays a dividend" is narrowed to what
re-derives: CIK 0001859199 reports dividends, under elements the registry does
not name.

Every fixture claim re-derived on main by running `filings::answering` and
`filings::attempted` over the three facts fixtures, from a throwaway outside the
workspace.
