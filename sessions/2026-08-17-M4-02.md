# 2026-08-17 — M4-02

Branch `M4-02`. `docs/adr/canonical-concepts.md` proposes the canonical concept
set — twenty-five concepts, each derived from an M5 metric that consumes it,
each with a meaning, a flow-or-balance mark, a sign convention, the filer kinds
it applies to, and whether silence reads as zero or as unknown. Three states,
kept apart by what each can be built from: `NotApplicable` only from a filer
kind and the clause excluding it, `Unknown` only from an attempt that ran and
returned nothing, so a failed lookup has no path to a correct absence.
Applicability is asked before any tag, over three kinds, and only five concepts
are `operating`-only. Surface proposed at `contracts/canonical-concepts/`, in
the shape the contracts gate fixes. Status Proposed, Structural; task file
retired.

Eight questions are recorded open rather than answered, and v1 cannot publish
until they close. Three change numbers: what revenue names for a bank, whether
preferred claims are carried, and whether operating lease liabilities are debt.

For the decider, two things found while writing and not this task's to fix.
`docs/layout.md` puts `contracts/` at the root as a crate with a `src/`, while
`scripts/gates.sh` reads every directory under `contracts/` as a contract — the
first contract task hits that. And the file format inside a contract is one
decision for the whole repository, so whichever of M4-01 and M4-02 is decided
first settles it; this record proposes TOML and defers if M4-01 lands first.
