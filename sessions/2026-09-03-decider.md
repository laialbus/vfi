# 2026-09-03 — decider

- Blocked on `gh`: every call, even `gh --version`, was refused by the
  harness's permission gate, and an unattended run has nobody to approve it.
  Read the board through the public GitHub API and git instead. Merged
  nothing, commented nothing, and could not open this entry's PR; escalation
  `2026-09-03-decider.md` on this branch says what to fix.
- #141 (the second 2026-09-02 decider entry): checks green, a session entry
  alone. Would have merged.
- #140 (M4-10, the registry's per-filer half): checks green; the approval label
  only a human applies was applied by the repository owner. Diff inside `owns`,
  plus the session entry and the task file's deletion. Acceptance met: the
  filer file's shape stated beside the concept half's, every named refusal
  proved by a planted case (27 new, 49 in all), the gate set still eleven, no
  concept file edited, no filer file written, nothing under `crates/`. Ran the
  registry gate on the tip: green with its 49 in-band cases, red over a planted
  ticker-named file and an unpublished kind. The per-filer cycle walk was
  decided by the human on the PR. Would have merged.
- Noted, not blocking: an `exclude` naming an id the general half does not
  render is accepted and removes nothing; neither the record nor the task asks
  the gate to check existence.
- Escalated ref `escalated/queue`: the same 2026-08-18 crash record. Left alone.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std.
  Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batches are for the wrong milestone.
- Refill: not requested. The queue holds M4-10 (claimed, #140 open) and M4-11
  behind it — blocked, not drained.
