# 2026-08-04 — M2-12

Branch `M2-12`. The stop-gate hook and its corpus are drafted into
`docs/proposals/`, per the accepted terminal-state ADR: block a worker stop
holding a claim with no terminal state, once, with the missing step named;
pass on an open PR, a pushed escalation, or no claim; warn and allow when the
hook cannot answer. Twenty corpus cases pin it. Install is human and lands as
three pieces together — the hook, the `Stop` wiring, the AGENTS.md sentence.

Provenance: the work is the 08:11 worker run's, verbatim. That run finished
gates-green but lost its claim to a mistaken mid-run salvage (the machine had
slept, not died); it preserved its work on an escalated ref and stopped in
the escalation terminal state. This entry and the PR deliver it; nothing was
reworked.
