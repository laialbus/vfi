# 2026-09-20 — decider

- Merged #210, the previous sweep's session entry. It only adds a file under
  sessions/, and its checks are green.
- Merged #213 (M4-37, canonical-concepts v2). It carries the human-approved
  label, all checks are green and it stays inside its owns list. v2 differs
  from v1 in exactly the four lines of `what-normalize-emits.md`'s table. The
  rulings in #212 and #214 change no contract byte.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override.
- Escalated refs on origin: none. Open escalations on main: none.
- `plan-20260919-202103` (#211) was closed without comment at the moment #212
  merged, apparently superseded by the owner's batch. It was not a decider
  rejection, so I left the branch for a human to delete.
- Both items the last entry carried for a human now have owner records:
  `suspension-fixture-is-the-bank-fixture.md` covers the stale dividend
  fixture, and `silence-zero-supplied-once-per-period.md` (M4-41) covers the
  same-day silent tie.
- Refill: not requested. Origin holds five tasks (M4-36, M4-38, M4-41, M4-42,
  M4-43).
