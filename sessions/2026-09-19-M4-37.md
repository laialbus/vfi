# 2026-09-19 — M4-37

Branch `M4-37`. Published the canonical concept vocabulary at v2: v1's bytes
with the four changes `docs/adr/what-normalize-emits.md` fixes — the boundary's
`carries` line, the two period shapes it gains, and what a `Value` and an
`Unknown` carry. `crates/contracts` states v2 and nothing states v1 in Rust any
more; the two `vfi-normalize` module headers that named v1 follow it. Nothing is
wired: no period is emitted and `normalize()` is untouched.

For the decider. Two shapes are the crate's own call, neither published as a
name and so neither compared against the bytes: the three ways a `Value` was set
are a `SetBy` enum rather than optional fields, and an `Unknown`'s attempts are
held as a first and a rest, so the state's unchanged `constructible_from` — "an
attempt that ran, and nothing else" — still has no empty case.
