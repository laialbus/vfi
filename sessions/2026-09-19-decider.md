# 2026-09-19 — decider

- Merged #205, the 2026-09-18 decider entry. Checks green; sessions only; what
  it says matches main.
- Merged #206, the planner's thirteenth M4 batch (M4-38). Its premise matches
  the milestones and the log. It traces to the accepted
  `what-normalize-emits.md`, the id is new, depends_on resolves to M4-36 and
  M4-37, the owns list can hold the diff, and the overlaps are ordered. Its
  32-period pin comes from the emit record, so it waits on the M4-36 ruling
  below.
- Closed #207 unmerged: it was the wrapper's PR for M4-36's deliberate stop.
  Its escalation is folded here byte-for-byte.
- Nothing deferred, no ADR accepted. `gh` and pushes needed the sandbox
  override.
- Escalated refs: `escalated/M4-36` is the stop folded here.
  `escalated/M4-36-20260918-233827` is the wrapper's record of an exit 0 with no
  PR, and I left it alone.
- Open escalations on main after this merges, one:
  `2026-09-18-M4-36.md`. Does a silence zero count toward Rule 1's
  "resolves to a `Value`"? Recommend: it does not. The accepted
  `what-normalize-emits.md` already counts 32 periods beside 96 silence zeros,
  and that only holds if silence is left out. The same ruling should confirm
  that a filer with no kind has no periods. This is Structural, a clause beside
  Rule 1, and a human's call because it settles a normalization reading. It
  parks M4-36 and, through depends_on, M4-38.
- Refill: requested (`planner/requested` at df5e51b). Origin holds three tasks
  (M4-36 to M4-38), no plan PR is open, and there is no planner escalation. Two
  of the three are parked behind the ruling, and M4-37 needs the human label.
