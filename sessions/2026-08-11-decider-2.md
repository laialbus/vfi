# 2026-08-11 — decider, second sweep

- Merged #91: the first 2026-08-11 decider session entry. Checks green,
  `sessions/` only.
- Merged #92 (`plan-20260811-201411`), the second M3 batch, M3-05 through
  M3-08. Premise holds against GOALS.md and the merge history — retrieval,
  host, rate and provenance criteria closed by #84, #88, #90; the funnel and
  the ledger remain. M3-05 and M3-06 trace to the accepted
  `filer-decision-ledger` ADR and restate its Decision faithfully; M3-07 and
  M3-08 trace to M3's funnel and verdict criteria. No id ever used before;
  the depends_on chain resolves inside the queue; each owns list holds its
  task's expected diff and names no protected path; the four overlap on
  `crates/fetch/` and are ordered into a chain for it.
- Nothing rejected, nothing deferred.
- Escalated refs: none on origin.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- No refill requested: #92's merge put M3-05 through M3-08 in the queue —
  not drained.
