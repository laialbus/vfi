# M1-05, branch M1-05

Scoped the language style rules with `paths` frontmatter: `rust.md` to
`crates/**/*` and `contracts/**/*`, `python.md` to `shell/**/*` — the paths
`docs/layout.md` names for each. Dropped both files' "not yet scoped" preamble;
the frontmatter is now the only statement of scope.

`git.md` and `comments.md` are unchanged: they already stated they are unscoped
by design, not pending. `shell/` does not exist yet, so the Python rule simply
will not load until it does.
