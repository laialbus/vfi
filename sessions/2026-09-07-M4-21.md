# 2026-09-07 — M4-21

Branch `M4-21`. `registry/filers/` gains its first file, `0002003750.toml` — the
filer `fixtures/fetch/every-fact-a-filer-reported` is recorded from — asserting
`kind = "operating"` and nothing else: no `include`, no `exclude`, no `assert`.
The ground sits in a comment beside the assertion, no field being invented to
hold it: the current-against-non-current subtotals its balance sheet states, the
revenue-to-cost-of-revenue-to-operating-income run its income statement makes,
and for each of the other four kinds the fact of this filer's that refuses it.

It is the first real filer in the tree with a kind, so the first that can reach
`NotApplicable` at all. No concept file changed, and the general mapping reaches
what it reached before.
