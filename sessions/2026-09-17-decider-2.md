# 2026-09-17 — decider, second sweep

- Merged #200, the first 2026-09-17 decider entry. Checks green, and its
  claims match main.
- Merged #201, the twelfth M4 batch (M4-34 to M4-37). The premise holds, every
  task traces to an accepted record, the ids are new, M4-36's dependency is in
  the batch, and M4-37's overlap is covered by `exclusive`. The figures the
  tasks pin re-derive against the ADRs and fixtures. One slip, noted on the PR:
  M4-35 credits the `preferred_equity` 0001859199 claim to the Rule 3 and
  silence records, but it is the emit record's.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override.
- Escalated refs on origin: none. Open escalations on main: none.
- New for a human: the accepted emit record's dividend-suspension fixture
  claims are stale. M4-33 made 0001778784 `bank`, and `short_term_investments`
  applies to `operating` only, so it is `NotApplicable` there at every period.
  This is a Structural record and needs a human decision before the fixture
  task is queued. Everything carried in the 2026-09-16 and first 2026-09-17
  entries still stands.
- Refill: not requested. Four tasks sit on origin/main.
