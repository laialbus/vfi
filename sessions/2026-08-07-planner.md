# 2026-08-07 — planner

- Branch: `plan-20260807-214208`. Queued M3-01 through M3-04, the first batch of
  M3; M2's last criterion closed with #69.
- M3-01 the fetch chokepoint and the check that it cannot reach an unlisted
  host, M3-02 the source's published rate limit, M3-03 ticker to filing history
  with provenance on every record, M3-04 an ADR proposal for the filer decision
  ledger.
- Ends there. The funnel and the ledger itself wait on M3-04's decision, and
  what fetch hands normalize is a contract, which needs its own ADR and the
  sign-off `contracts/` carries.
