# 2026-08-10 — M3-02

Branch `M3-02`. `vfi-fetch` now holds to the access policy EDGAR publishes,
stated once in `policy.rs`: ten requests a second, cited to the page that
publishes it, with the spacing derived from the rate rather than written down
beside it. `Pace` hands out turns from behind one `Arc`, so callers holding
clones of it share the bound — the published rate is over the source,
"regardless of the number of machines used to submit requests". Every `Cleared`
now carries the declaration of who is asking; it is the user's to supply and
arrives as a parameter, and one that is blank, not ASCII, or carrying a line
ending is refused where it is given rather than sent.

`tests/pacing.rs` proves the spacing against a clock the test owns. Nothing
sleeps, and no case reaches a source.

One thing for whoever fetches next: sec.gov answers an undeclared automated tool
with 403, which is what this workspace's fetch tools are. The published wording
was read through search restricted to sec.gov rather than by rendering the page,
and only the rate and the user-agent requirement were taken from it.
