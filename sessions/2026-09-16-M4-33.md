# 2026-09-16 — M4-33

Branch `M4-33`. `registry/filers/` gains `0001778784.toml` — Provident Bancorp,
Inc. /MD/, the filer `fixtures/fetch/a-filer-that-suspended-its-dividend` is
recorded from — asserting `kind = "bank"` and nothing else: no `include`, no
`exclude`, no `assert`. The argument sits in a comment beside the assertion.

Both stretches read `bank`: balance sheets at 2021-12-31, 2022-12-31,
2024-12-31 and 2025-09-30, and the income runs they close, all closing to the
dollar. The dividend is not read as a ground. The file says the clause's
"ordered by liquidity" is not in the facts, which carry amounts and not line
order; what it reads instead is that no line is split by term.

Every figure was re-derived against the recorded company facts document by a
throwaway script outside the tree.
