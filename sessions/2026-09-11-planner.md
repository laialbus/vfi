# 2026-09-11 — planner

Queued M4-25 through M4-29, the ninth M4 batch, on the decider's refill
request. M4 stays active: the retrieval refuses company facts for nineteen
filers in twenty, nothing resolves through the stage, Rule 3 of the accepted
alignment record waits on a human, and three of the golden fixtures the
milestone names have no filer data to be written against.

M4-25 publishes fetch → normalize v2 as `docs/adr/fetch-normalize-v2.md`,
accepted 2026-09-10, states it — the padded key from either spelling,
`report_period_end` joined from the history — with the number-spelled fixture
the record puts in the same task, and deletes the cik escalation; exclusive,
since it writes a contract. M4-26 admits the cover-page entry on the accepted
alignment record's condition, which v2 makes stateable, so `shares_outstanding`
can reach a value at the period its filing is about and at no other. M4-27 to
M4-29 record the negative-equity, dividend-suspension and non-ordinary-shape
filers the milestone names among its golden fixtures, chained only because
each writes the harness header.

Ends before Rule 3 in code, Rule 1, the restatement fixture and the record on
what the stage emits, all of which wait on M4-23, parked on a human; and before
any kind assertion for the filers recorded here, which waits on the recordings.
