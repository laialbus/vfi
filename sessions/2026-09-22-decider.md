# 2026-09-22 — decider

- Merged #215, the 2026-09-20 decider entry. Checks are green, it adds only a
  sessions file, and what it says matches main.
- Merged #216 (M4-36, `periods::periods`: Rule 1). Checks are green, it stays
  inside `crates/normalize/`, and the task file is retired. The 32/19/13 count
  and all 18 strays are re-derived and pinned. No gate, contract, registry byte
  or golden fixture moves. The worker read "asked through the entries that
  answer the period" as narrowing before Rule 3, not filtering after it. I
  accepted that as the literal reading; both readings give 32 here.
- Closed #217 unmerged. It was M4-42's deliberate stop, and its escalation is
  folded here byte-for-byte, as #207's was.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` and pushes needed
  the sandbox override.
- Escalated refs: `M4-42` is the stop folded here, and nothing else is on
  origin. `plan-20260919-202103` (#211, closed by the owner) is still on origin
  for a human to delete.
- Open escalations on main after this merges: one.
  `2026-09-21-M4-42.md`: the stand-in record claims "the year 2025" is the
  silence zero on CIK 0001778784, but no filing answers 2025-01-01..12-31.
  Recommend the record's Enforcement line be corrected to "the 2025 periods
  the fixture carries", which do re-derive. Then M4-42 proceeds as written.
  This is Structural because it edits an accepted record's claim, so it is the
  owner's call. It parks M4-42.
- Refill: not requested. Origin holds four tasks (M4-38, M4-41, M4-42, M4-43).
