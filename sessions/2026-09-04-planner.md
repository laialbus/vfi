# 2026-09-04 — planner

Queued M4-16 through M4-18, the sixth M4 batch, on the decider's refill request.
M4 stays active: the registry is data on disk and the vocabulary is a type, but
nothing constructs a state, nothing reads a fact, and nothing resolves.

The batch is the resolution procedure `docs/adr/candidate-choice.md` decides,
split where that record splits it. M4-16 asks applicability first, serving the
criterion that each concept resolve to one of three states with the two absences
never confused — `NotApplicable` has no other way in. M4-17 answers which facts
answer a period, and M4-18 settles the eligible rules into a `Value` or an
`Unknown`; both serve the criterion asking that choosing among candidates in one
filing follow a stated rule, and M4-18 also the one asking a resolved value to
record its tag, its filing and the rule that set it.

Ends before the stage is wired and before the six named fixtures.
`docs/adr/period-alignment.md` is proposed by M4-14 and not accepted, so which
periods a filer has and which filing answers each is undecided; until it is, the
period is the caller's to name, which is the seam the accepted record draws.

For the decider: all three own `crates/normalize/`, so they chain behind M4-13 —
M4-11, M4-13, M4-16, M4-17, M4-18 in order, one worker at a time on that crate,
with M4-14 the only parallel work. One gap found while deriving, not queued
because nothing in this batch needs it: the registry interface takes a filer's
kind as an input and the accepted record gives it no answer for one, so nothing
yet reads the `kind` a filer's file asserts.
