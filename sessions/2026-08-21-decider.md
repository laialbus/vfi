# 2026-08-21 — decider

- Merged #114: the 2026-08-20 decider session entry, carrying the
  fetch-normalize-contract acceptance edit. Checks green, within the
  decider's own write surface.
- Merged #115 (plan-20260820-141936): queued M4-03, M4-04, M4-05. Premise
  holds — M4 active, both M4 records accepted but nothing delivered,
  `contracts/` has never existed. Ids fresh across all refs, depends_on
  resolves in-batch, the `contracts/` overlap between M4-03 and M4-05 is
  ordered, and each owns list holds its task's diff (the contracts gate
  lives in `scripts/gates.sh`, which M4-03 owns). M4-03 and M4-05 are
  exclusive, so the batch single-threads; both will need the
  human-approved label at merge, as their bodies state.
- Nothing rejected, nothing deferred.
- Escalated ref `escalated/queue`: the same crash record earlier sweeps
  noted — `tasks.sh available` failed at c436ee8, no stderr. Left alone;
  the run retries on its own.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batches merged in #108 and #115 are
  for the wrong milestone.
- Refill: not requested — the queue holds M4-03, M4-04 and M4-05.
