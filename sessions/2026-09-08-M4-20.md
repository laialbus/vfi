# 2026-09-08 — M4-20

Branch `M4-20`. Fixture `a-filer-that-changed-its-fiscal-year`: CIK 0001859199,
4232 facts, recorded through `crates/fetch/src/bin/record.rs`. The filer moved
its year end from 30 April to 31 December, so its own dates carry a 245-day
transition period between years at their own length, and quarters on two
calendars that never coincide. Why this filer fits is in the harness header
beside why the first one was chosen. `expected` was written from the recording
by a throwaway reader outside the workspace, as the first case's was.

Nothing reads the case yet. The stage, the first facts case, the normalize
fixture and the baseline are untouched, and the lockfile did not move. Proved
by hand both ways: a fact removed from the recording and a value respelled each
turn the case red. The recording holds no collision.

For the decider. Escalation `2026-09-08-companyfacts-cik-as-a-number.md` — EDGAR
spells a company facts document's `cik` as a bare number for about nineteen
filers in twenty, and the stage refuses every one of those documents. It shaped
this task: the filer had to come from the ten in 217 I sampled that spell it as
a string. No gate sees it, because both facts fixtures are recordings of the
minority. Not fixed here; one run is one task.

The recording ran with the sandbox off, as M4-07's did and for the same reason.
So did the search that found the filer — EDGAR's full-text index of transition
reports, then one companyfacts head per candidate — none of which is committed.
Two recordings taken apart were byte-identical.
