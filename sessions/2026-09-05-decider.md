# 2026-09-05 — decider

- Merged #149, the second 2026-09-04 decider entry: checks green, a session
  entry alone, its claims matching main.
- Merged #150, the planner's sixth M4 batch (M4-16 to M4-18): premise checked
  against the milestones and the merge history, every task traced to a
  criterion or to the accepted candidate-choice record, ids fresh, depends_on
  resolving, owns lists able to hold the diffs, overlaps ordered behind M4-13.
  The gap its body names — nothing reading a filer's asserted kind — is closed
  by M4-11's `kind_of`; nothing in the batch changes.
- Merged #151 (M4-14) and accepted `docs/adr/period-alignment.md`, Structural,
  flagged for human review: every fixture claim re-derived from the raw
  companyfacts and submissions documents, the v2 field proposed and not
  written. Its Decision review, Status and Decided date ride in this PR.
- Merged #152 (M4-11): checks green, diff inside the owns list plus the session
  entry and the task file's deletion; no protected path, gate and registry data
  untouched; the digest computed in one place, the gate's own header saying it
  computes none. The three readings the worker flagged stand.
- Nothing rejected, nothing deferred. `gh` needed the sandbox override after
  its first call, as before.
- No escalated refs on origin; nothing to fold.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std.
  Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Refill: not requested. The queue holds four — M4-13, M4-16, M4-17, M4-18 —
  which is above the line.
