# 2026-09-09 — decider

- Merged #163, the 2026-09-08 decider entry: checks green, a session entry
  alone, its claims matching main; the refill it requested was consumed and
  came back as #164.
- Merged #164, the planner's eighth M4 batch (M4-22 to M4-24): premise checked
  against the milestones and the merge history — every task it counts as
  landed is in the log, both rulesets accepted; each task traced to a
  criterion, an accepted record, or the open escalation; the two readings of
  Rule 3 re-read in the accepted alignment record and the accession it names
  found in the merged fixture; the second facts fixture confirmed as CIK
  0001859199; ids fresh; no depends_on to resolve; owns lists able to hold
  their diffs, with no overlap among the three or with M4-19. Touches only
  `tasks/` and `sessions/`.
- Merged #165 (M4-18): checks green; diff inside `crates/normalize/` plus the
  session entry and the task file's deletion; lockfile, `normalize`, golden
  fixture, contract surfaces, registry data and gates unmoved; the eleven
  answering tests intact after their helpers moved to a shared module.
  Re-read against the accepted candidate-choice record: the four steps in its
  order with nothing between, applicability first, an assertion total, a
  difference's operand settled by the same procedure, agreeing survivors
  `Unknown`, the six decline reasons and no seventh, the silence sweep read off
  the vocabulary's definition, both live contests pinned under a named kind.
  The three flagged shapes stand.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` ran inside the
  sandbox throughout this run without an override.
- No escalated ref on origin.
- Open escalations on main, one: `2026-09-08-companyfacts-cik-as-a-number.md`.
  Recommend as before: Structural; M4-24, now queued, is the record that acts
  on it, and the task that publishes the v2 surface deletes the file. It names
  no task, so it parks nothing.
- For a human, three questions carried:
  - Rule 3 stated two ways in the accepted alignment record: now queued as
    M4-23. Nothing to do until that record is proposed.
  - How a partial supersession is recorded, raised by the planner. The ADR
    README says an accepted record is never edited; the template lists a
    `Superseded by` status. Recommend at M4-23's acceptance the alignment
    record keeps its bytes and its Status, and the superseding record names
    in its own header exactly which rule it replaces; whether the old record's
    Status is ever flipped is a human's call, since the README forbids the
    edit and the template invites it.
  - What the stage emits once wired, raised by the planner and sharpened by
    M4-18: the vocabulary's surface carries one state per concept per filer
    with no period, and its `Value` asks for a source tag and a filing that an
    asserted value and a silence-supplied zero do not have. Recommend one
    Structural record, ahead of the wiring task and after Rule 1's, deciding
    the per-period shape and what those two fields hold in those two cases.
    Neither is a worker's to invent, and M4-18 rightly did not.
- Refill: not requested. The queue on origin holds four — M4-19, M4-22, M4-23,
  M4-24 — which is above the line.
