# 2026-08-03 — M2-07

Branch `M2-07`. `scripts/gates.sh` gains the `scripts` gate, first in the set
and the one name AGENTS.md's list does not carry: it is over the operational
scripts themselves, which every other gate is reached through.

Two halves. Every file under `scripts/` parses under the shell its shebang
declares, and an executable that declares none is refused rather than skipped.
Every corpus under `scripts/tests/` runs, read off the directory rather than
listed — a case is one run of the script against a scratch repository with a
real bare remote, pinning exit code, stdout, stderr, and the refs origin holds
afterwards.

`tasks.sh` gets 46 cases, covering what PR #18 proved by hand: malformed
frontmatter refusing the whole listing at 3, exclusive gating in every claim
state, dependency gating, claim-then-verify, and the five exit codes kept
apart. The proof throws away the exclusive value inside `tasks.sh` — a change
that still parses and touches no crate — so only this gate can catch it.

`scripts/tests/` is a new home and `docs/layout.md` names it, per the task.

`scripts/gates.sh` is protected and this task owns it, so the pull request needs
the `human-approved` label.
