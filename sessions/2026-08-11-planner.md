# 2026-08-11 — planner

- Queued M3-05 through M3-08, the second and last M3 batch. M3's retrieval,
  host, rate and provenance criteria closed with #84, #88 and #90; what remains
  is the funnel and the decision ledger.
- M3-05 the ledger's interface, record and in-memory implementation, M3-06 the
  append-only journal behind it, M3-07 the seed set and the metadata gate with
  every verdict recorded, M3-08 the funnel closed — history for survivors only.
  M3-05 and M3-06 implement the accepted `filer-decision-ledger` ADR.
- All four own `crates/fetch/`, so they run as a chain rather than in parallel;
  nothing in the crate splits into non-overlapping halves without both tasks
  needing `lib.rs`.
- Ends at the funnel's close, which is M3's last unmet criterion. What follows
  is M4 and the fetch → normalize contract, which carries its own ADR and the
  sign-off `contracts/` requires.
