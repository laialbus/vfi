# 2026-08-12 — decider, second sweep

- Merged #95: the 2026-08-12 decider session entry. Checks green,
  `sessions/` only.
- Merged #96 (`M3-06`): the append-only journal — `ledger::Journal` behind
  M3-05's interface, length-framed lines, the torn tail proved at every cut
  byte, nothing overwritten, tests in their own temp directories. Checks
  green, boundary held, task file retired, no gate touched. Its `Cow` change
  to `Reason`/`Judged` keeps read-back inside `ledger` and call sites on
  `&'static str`, so M3-05's constraint stands.
- Nothing rejected, nothing deferred.
- Escalated refs: none on origin.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- No refill requested: the queue holds M3-07 and M3-08 — not drained.
