# 2026-09-06 — decider

- Merged #153, the 2026-09-05 decider entry: checks green, its claims matching
  main, and the acceptance of `docs/adr/period-alignment.md` it carries touching
  only the Decision review, Status and Decided lines.
- Merged #154, the owner's ruling closing M2-03 and M2-14: human-authored,
  label applied by the owner, one protected file, diff matching the body.
  Constitutional tier, decided by the human; merged as transport, not accepted
  by the decider. One gap for a later run: the ruling's condition that a
  subsumed guard "says so in place" is not yet met by `own_branch_only`'s own
  comment in `scripts/tasks.sh`, which still says only what it keeps out of
  reach.
- Merged #155 (M4-13): checks green, diff inside the owns list plus the session
  entry and the task file's deletion; the task was exclusive and no other task
  branch was in flight; `scripts/gates.sh` carried the owner-applied label.
  Re-derived from the branch: 75 entries, 30 exact, one `answers` line, three
  concepts with no exact entry, none without a reading. The seven cases come
  out as the candidate-choice record walks them; the balance measure is read
  off the published surface, where all 28 concepts carry it before any
  sub-table. Four refusals, five proofs of catch, no case dropped — the one
  edited case swapped its undeclared field because `reading` is now declared.
  The reader refuses what the gate refuses and carries both fields out unread.
  The one judgement a fixture should check first: the cover-page share count
  marked `stand_in` against a meaning that says "at or near the period end" —
  the reading the record's own live fixture case needs, and argued in the file.
- Nothing rejected, nothing deferred. `gh` needed the sandbox override after
  its first call, as before.
- One escalated ref on origin, `escalated/M4-13-20260906-064243`: the wrapper's
  crash record ("agent exited 1"), carrying nothing but its note. Left alone;
  the task retried and landed as #155.
- Open escalations on main: none. #154 closed the two carried since 2026-08-07.
- Refill: requested, `planner/requested` at c6b0fc1. The queue holds three —
  M4-16, M4-17, M4-18 — which is at the line; no `plan-` PR is open and no
  planner escalation sits on main.
