# 2026-09-15 — M4-29

Branch `M4-29`. New case `a-filer-whose-statements-have-no-gross-profit`: CIK
0000798783 (Universal Health Realty Income Trust), 14724 facts, key spelled as
the bare number, recorded live through `crates/fetch/src/bin/record.rs`. No fact
in the document sits under `us-gaap:GrossProfit` or under any of the four
cost-of-revenue elements the registry's difference forms for `gross_profit`
subtract, nor under `AssetsCurrent`, `LiabilitiesCurrent`, `InventoryNet` or any
of the four `short_term_investments` elements. Why it reads as a `reit` against
the vocabulary's clause is in the harness header. `expected` was written by a
throwaway outside the workspace, which reproduced all five existing cases'
`expected` byte for byte. Proved by hand: one digit changed in the 2025
operating income turns the case red. Two recordings taken apart were
byte-identical, and the recording holds no collision.

For whoever asserts the kind. The lease revenue is tagged
`OperatingLeasesIncomeStatementMinimumLeaseRevenue` and `PercentageRent` only
through the filing reporting to 2018-12-31; after that the line sits under a
company extension EDGAR's collation does not carry, and what is left is
`Revenues` with `LessorOperatingLeasePaymentsToBeReceived` beside it.
`us-gaap:InvestmentOwnedAtFairValue` carries 26 facts from 2022-12-31 on — the
equity-method LLCs, about 9m against 565m of assets, not a portfolio.

Rejected on the way: Global Self Storage (CIK 0001031235) is half the size and
clean on every element, but its four earliest filings carry the predecessor
closed-end fund's 2015-12-31 balance under `AssetsNet` and
`InvestmentOwnedAtFairValue`, so its earliest instant is an investment
company's. Plymouth Industrial REIT tags no operating income in its recent
years. The search ran on XBRL frames, then one submissions and one company facts
request per candidate. The recordings ran with the sandbox off, because the
stage's transport does not use the sandbox proxy. None of the search is
committed.
