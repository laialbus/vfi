# 2026-09-16 — M4-32

Branch `M4-32`. Proposes `docs/adr/what-normalize-emits.md`: per filer, the
periods Rule 1 admits, keyed by their dates alone, and every concept in one state
at each. A `Value` names which of three ways set it: `read`, `asserted` or
`silence`. An `Unknown` carries each answering filing's attempt under its
accession. It is published as `canonical-concepts` v2, and it takes up M4-30's
registry version on a silence value. The silence record read is the text #192
accepts, still open at the time.

For the decider. The accepted Rule 3 tables pin a silence zero for
`short_term_investments` at durations, so every row carries concepts its shape
cannot answer. The record prices that and does not reopen it. It also carries
something for a human: the suspension fixture reads both dividend concepts as
silence zeros in Q4 2020 and Q4 2021, while the filer's year-to-date dividends
paid rise across both quarters.

Fixture claims were re-derived on `71365cc` by a throwaway program under the
ignored `target/`, run over the five facts fixtures.
