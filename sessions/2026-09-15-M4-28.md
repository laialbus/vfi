# 2026-09-15 — M4-28

Branch `M4-28`. New case `a-filer-that-suspended-its-dividend`: CIK 0001778784
(Provident Bancorp), 12083 facts, key spelled as the bare number, recorded live
through `crates/fetch/src/bin/record.rs`. A dividend is reported under
`CommonStockDividendsPerShareDeclared` and `PaymentsOfDividends` at each report
period end from 2020-06-30 to 2022-12-31. None of the four registry elements
carries a fact at any report period end from 2023-03-31 to 2025-09-30. The
header gives the periods. `expected` was written by a throwaway outside the
workspace, which reproduced two existing cases' `expected` byte for byte. Proved
by hand: dropping one dividend fact from `expected` turns the case red. Two
recordings taken apart were byte-identical, and the recording holds no collision.

For whoever writes the normalize fixture: the filer's own zeros sit under
`DividendsCash` (2023) and `Dividends` (2024 on), which the registry does not
map. The filings at 2019-09-30, 2019-12-31 and 2020-03-31 are silent because
dividends had not yet begun, not because they were suspended. The 10-K year to
2022-12-31 declares 0.12, the same as the nine months, and no quarter to that
date is reported.

Rejected on the way: Medalist Diversified REIT's silence is a move to
`DividendsCommonStock` and `CommonStockDividendsPerShareCashPaid`, not a
suspension. The search ran on XBRL frames plus one company facts request per
candidate. The recordings ran with the sandbox off, because the stage's
transport does not use the sandbox proxy. None of the search is committed.
