# 2026-08-14 — M3-10

Branch `M3-10`. `crates/fetch/src/bin/record.rs` records a golden fixture
through the stage itself: the URL leaves by `Egress` with `Https` under it, at
the shared pace, and the body lands unchanged at the path the URL names under
the case directory it was given. Who is asking arrives as an argument, with no
default to fall back on. `tests/golden.rs` names it as the way to record, in
place of the curl line, and `docs/layout.md` gives a development entry point its
home.

What was recorded: `a-ticker-edgar-does-not-know`, one document —
`https://www.sec.gov/files/company_tickers.json`. EDGAR answered 200 with
794,753 bytes, sha256
`5fa0f09f8e8dd83ad338adb5ac56403d0ac1df122bf4dadf772984c4a749d783`, which is
byte for byte the recording curl took on 2026-08-11. The map still does
not name VFIQ, over 10,387 rows, so `expected` stands as written. Neither thing
the task said to watch for turned up: the transport got no bytes curl did not,
and the recording has not drifted from what EDGAR publishes. The fixture diff is
empty for that reason, and this entry is the record that the run reached the
source.

For the decider. This run's sandbox blocks direct DNS and routes shell traffic
through a local proxy. `Https` does not use one — it connects to the host the
list cleared, which is the property M3-09 was for — so the single recording
command ran with the sandbox off. Nothing else in the run did, and nothing added
here makes a test reach the network.
