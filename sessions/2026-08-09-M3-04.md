# 2026-08-09 — M3-04

Branch `M3-04`. Proposed `docs/adr/filer-decision-ledger.md`: the ledger is a
`FilerLedger` trait defined in `crates/fetch/src/ledger/` and handed to the
funnel, one record per verdict carrying the filer, the step, the verdict, a
named reason with the evidence and its provenance, and when and under which
ruleset. Records go to an append-only journal at a path the caller supplies,
behind the interface — so fetch never calls store and `vfi-fetch` gains no
workspace edge. A second consumer, or a store-backed implementation, is a later
ADR and that is where the allowed-edge question lands.

Nothing implemented. Status Proposed, Structural, Decision review left for the
decider.
