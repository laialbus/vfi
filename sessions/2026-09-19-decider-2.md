# 2026-09-19 — decider, second sweep

- Merged #209, the owner's ruling on the M4-36 escalation the last sweep
  carried. Checks green; it touches `tasks/M4-36.md` and the escalation file
  and nothing else, so no protected path and no label is in question. It rules
  that a `Value` a silence reading supplied makes no period exist under Rule 1,
  confirms that a filer with no kind established has no periods, and deletes
  the escalation — the resolution being the owner's, not mine. No accepted
  record, contract or gate moves. M4-36 was unclaimed, so nothing collided.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` and pushes needed
  the sandbox override.
- Escalated refs on origin: none. The two `escalated/M4-36…` refs the last
  sweep noted are gone from the server.
- Open escalations on main: none. With the M4-36 file gone, M4-36 and M4-37 are
  claimable and M4-38 stays ordered behind both.
- Still waiting on a human, neither being an escalation file, and both carried
  because the last entry dropped them:
  - The emit record's dividend-suspension fixture claims are stale — M4-33 made
    CIK 0001778784 `bank` after they were derived. Structural; M4-38 declines
    to pin them for this reason. Recommend the record be corrected before the
    fixture task is queued.
  - Two same-day filings that are both silent come to `Unknown` under Rule 3's
    tie clause, while the silence record reads that case as the zero — two
    accepted records disagreeing at one edge. Structural. Recommend one record
    be made to yield to the other; M4-38 crosses whatever `stands` answers and
    changes nothing.
- Refill: requested (`planner/requested` at 90df9b9). Origin holds three tasks
  (M4-36 to M4-38), no plan PR is open, and there is no planner escalation.
  M4-37 still needs the human-approved label when its run opens a PR.
