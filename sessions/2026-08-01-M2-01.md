# 2026-08-01 — M2-01

Branch `M2-01`. `scripts/gates.sh` runs the two gates that have machinery today
— the workspace builds, the workspace tests pass — and then proves each one by
injecting the violation it exists to catch into a scratch copy of the tree and
requiring the runner there to go red and name that gate. An untouched copy runs
first as a control, so a copy that fails for its own reasons cannot read as a
gate catching something. CI's `gates` job runs the same command with no
arguments.

Both owned paths are protected, so the pull request needs the `human-approved`
label. The M1-08 escalation still stands: the installed hook exits 1 under the
`python3` that `.claude/settings.json` invokes, so nothing refused these writes
locally — the server-side check is what holds them.
