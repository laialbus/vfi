# 2026-09-10 — planner

Queued M4-25 through M4-27, the ninth M4 batch, on the decider's refill request.
M4 stays active: nothing resolves through the stage, Rule 3 waits on a human
restating M4-23's counts, and the retrieval refuses the company facts document
for most filers.

M4-25 publishes `fetch-normalize` v2 as the decider accepted it in #171 — the
filer's key in one spelling and the report period end beside it — and moves the
boundary onto it: the type, the retrieval, both facts cases re-rendered, the
normalize test reader, and the cik escalation closed. Serves the criterion that
filings are mapped into the canonical set, which the v1 reader holds for one
filer in twenty. M4-26 records the first number-spelled filer as the third facts
case, the fixture the accepted record says pins the key. M4-27 admits the
cover-page entry on the accepted alignment record's condition, now that the
boundary carries the field the condition names — the right period basis for
choosing among candidates.

Ends before Rule 3 and Rule 1, which wait on M4-23 and on a human; before the
record on what the stage emits, which the decider placed after Rule 1; and
before the funnel's facts step, which no M4 criterion needs. M4-25 is exclusive
and its merge needs the label.
