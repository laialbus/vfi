# 2026-09-09 — M4-22

Branch `M4-22`. `registry/filers/` gains `0001859199.toml` — reAlpha Tech Corp.,
the filer `fixtures/fetch/a-filer-that-changed-its-fiscal-year` is recorded
from — asserting `kind = "operating"` and nothing else: no `include`, no
`exclude`, no `assert`. The ground sits in a comment beside the assertion, in
the fields the per-filer half already has.

Its facts stand on two calendars, so the comment argues each side of the change
on its own, and both sides read `operating`: current-against-non-current
identities that close to the dollar at 2022-04-30, 2023-04-30 and 2024-12-31,
and the revenue-to-cost-of-revenue-to-operating-income run over both April
years, the transition period, 2024 and 2025. Each of the other four kinds is
refused by a fact on either side. REIT is the one this filer needs argued rather
than dismissed — property is 3,816,149 of 6,886,666 of assets at 2022-04-30 —
and what refuses it is the presentation, not the size of the line.

Second filer in the tree with a kind, so the second that can reach
`NotApplicable`. No concept file changed and the first filer's file is
untouched.
