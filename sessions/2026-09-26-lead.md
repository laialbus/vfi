# 2026-09-26 — lead

Direct human commission: diagnose and resolve the M4-44 stop.

- The benchmark proof of catch inserts one 21-byte allocation into the
  stage's entry point; against 283,306 allocations per call it moves the
  counted work by one part in three hundred thousand, and `work 1.05` cannot
  see it. `benchmark-proof-scales-to-the-baseline.md`: the runner reads the
  baseline's `bytes` and `passes` and allocates a tenth per call, so the proof
  goes red at any size; thresholds untouched; target unchanged. Queued as
  M4-50, exclusive, on the gate runner — its PR needs the `human-approved`
  label. M4-44 now depends on it, and its acceptance says so.
- The 2026-09-25 decider's flag on #231: a filing that lost on `form` beneath
  a tie on `filed` is not carried, by the tie record's own reasoning. Noted in
  the ADR index; no record edited.
- The escalation rides on `escalated/M4-44-benchmark-proof`; M4-50 deletes the
  file if a sweep lands it first.
