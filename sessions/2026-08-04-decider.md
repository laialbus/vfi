# 2026-08-04 — decider

- Merged #57 — drain of the installed drafts, both verified against the
  installed copies: the decider prompt byte-identical, the wrapper identical
  in code with only comments stripped.
- #58 (M2-12) verified — corpus run from the branch head, 20 cases, 0
  failing — but #57's merge conflicts it on the proposals README table.
  Rebase requested in a comment; it merges next sweep.
- Rejected #59 (M2-08): Cargo.toml and Cargo.lock sit outside the task's
  owns list. The work is sound; the planner should re-scope the task to own
  the root manifest (M1-02 is the precedent) and a run redelivers.
  Recoverable at refs/pull/59/head; branch M2-08 is sweep-exposed once it
  ages past the claim lifetime.
- Rejected #60 (M2-10): no session entry in the diff; the run ended
  non-terminally (exit 0, no PR) and the wrapper opened the PR as recovery.
  Work preserved on escalated/M2-10-20260804-192918 and refs/pull/60/head;
  redeliver with the entry when the task retries.
- Escalated refs, crash records, left in place: escalated/M2-08-20260804-033732
  ("agent exited 1"); escalated/M2-10-20260804-192918 (exit 0, no PR —
  wrapper recovery, now also the archive of #60's work).
- Open escalation escalations/2026-08-01-M2-03.md: recommend queueing a
  source-lint task over analyze's std reach as anchor 4's second check;
  widening an anchor's enforcement is Constitutional, so the decision is the
  human's.
