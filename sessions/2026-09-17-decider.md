# 2026-09-17 — decider

- Merged #196, the 2026-09-16 decider entry accepting the emit record. Checks
  green, and its claims match main.
- Merged #197, M4-33, `registry/filers/0001778784.toml`, `kind = "bank"`. Every
  figure re-derives against the recorded document: both liabilities sides, all
  four asset sides, the four income runs from interest income to net income, the
  two deposit and maturity tables, and the 8,632,000 falling due within twelve
  months beyond `ShortTermBorrowings` against the 2023 FHLB repayment. Both
  stretches read the same, and the dividend is not read as a ground.
- Merged #198, M4-31, `registry/filers/0002011954.toml`, `kind = "operating"`.
  Likewise: five balance sheets, the current subtotals that close and the two
  that do not, four income runs, the nonoperating and cash-flow identities, and
  the goodwill roll. Both equity elements read `operating`; the deficit and the
  element change are not read.
- Merged #199, M4-29, the `a-filer-whose-shape-has-no-gross-profit` case, CIK
  0002033615. 2591 facts, three taxonomies, four units, eight accessions and 53
  filings on one page, all as recorded. None of the five element names
  `gross_profit` is reached by appears under any taxonomy, nor do the elements
  the other five narrow-clause concepts are reached by. The header's balance
  sheets, income runs and the -26,977 tax figure re-derive. It replaces the work
  rejected as #194, on a different filer.
- Nothing deferred, nothing Constitutional seen. `gh` needed the sandbox
  override throughout.
- Escalated refs on origin: none. Open escalations on main: none.
- Carried for a human, unchanged from the 2026-09-16 entry, which now sits on
  main: the accepted emit record and its two priced costs (the wrong-shape
  silence zeros, the conditional dividend zeros); the Rule 3 and silence
  records' Structural reviews; M4-27's premise; the unmapped dividend elements
  on 0001859199 and 0001778784; the #181 ruling on v2's descriptive lines; how
  a partial supersession is recorded; and `company_facts` fetching the history
  itself.
- Refill: requested, at `planner/requested` on 5f918a1. The queue on origin is
  empty, no `plan-` PR is open, no planner escalation sits on main, and the ref
  did not exist — the previous run's request was consumed.
