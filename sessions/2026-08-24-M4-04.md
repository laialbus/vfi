# 2026-08-24 — M4-04

Branch `M4-04`. Proposed `docs/adr/canonical-concepts-open-questions.md`, which
closes the seven questions the canonical concept vocabulary left open — the
eighth having closed with the fetch → normalize contract. Three tests carry the
answers, stated once so the four questions with one shape cannot get four
answers.

What it changes, so the review knows where to look: three concepts join the set
(`preferred_equity`, `preferred_dividends`, `short_term_investments`), two filer
kinds join the closed set (`reit`, `investment_company`, and a regulated utility
is an `operating` filer), six concepts change meaning, and three of the five
zero silence readings become `Unknown` — the dividend pair keeps its zero, each
member supported by the other.

Nothing implemented. The accepted record is untouched. M4-05 can publish v1.
