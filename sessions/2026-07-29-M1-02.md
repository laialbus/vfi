# 2026-07-29 — M1-02

Task: M1-02, create the Rust workspace, one crate per stage.
Branch: M1-02.

Added the root workspace manifest and four empty library crates — `vfi-fetch`,
`vfi-normalize`, `vfi-analyze`, `vfi-store` — under `crates/<stage>/`, with no
dependency edges between them. Version and edition are inherited from
`[workspace.package]` so each is stated once. `Cargo.lock` is cargo's, and
`target/` is now ignored.

`cargo build` succeeds from the workspace root (cargo 1.97.1). This re-runs the
task that stopped earlier for want of a toolchain; the toolchain is installed.
