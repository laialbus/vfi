# 2026-08-28 — decider

- Merged #126: the 2026-08-26 decider entry.
- Merged #127 (M4-06): checks green, diff inside owns plus the session entry
  and the task file's deletion. The type matches the published v1 bytes
  field-for-field — the filer's two, the fact's eight in order, both period
  shapes — with `facts` correctly the held collection, not a named field. The
  drift check rides the tests gate; `scripts/gates.sh` gains only the two leaf
  edges onto `vfi-contracts`; nothing weakened. The protected path merged
  under the human-approved label, applied by the human on 2026-08-28.
- Nothing rejected, nothing deferred.
- Escalated ref `escalated/queue`: the same crash record earlier sweeps noted.
  Left alone; the run retries on its own.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std's fs, net,
  time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batches (#108, #115, #125) are for the
  wrong milestone.
- Refill: not requested. The queue holds M4-07 and M4-08.
