# 2026-08-07 — M2-14

Branch `M2-14`. Reading the queue now refuses three structures that a human eye
used to catch: two tasks whose `owns` overlap with neither ordered by
`depends_on` nor marked exclusive, a `depends_on` cycle, and a task file not
named for its id. `read_frontmatter` carries `owns` as a fourth field to make
that possible. `tasks.sh check` is the read on its own, reaching nothing outside
the tree, and `gates.sh` gains a `queue` gate that runs it over `tasks/`.

One existing case moved: an id with a slash is now refused at the queue read
rather than by `own_branch_only`, which leaves that guard unreachable. See the
escalation.
