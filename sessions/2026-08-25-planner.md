# 2026-08-25 — planner

Queued M4-06, M4-07 and M4-08, the third M4 batch. M4 stays active: both
surfaces are published now, but nothing of the mapping is built and normalize is
still the stub that hands a filing back unchanged.

M4-06 creates the crate the layout doc reserved and puts the fetch → normalize
surface in it as the type the stages compile against, checked against the
published bytes so the two cannot drift. M4-07 retrieves a filer's company facts
through that type and pins the first of them as a golden fixture. M4-08 proposes
the registry that turns a filing tag into a canonical concept.

Ends there, on two boundaries rather than one. The rule for choosing among
several candidates in one filing waits on M4-07: the fetch → normalize record
could not settle by reading whether a dimensioned fact crosses, and left that to
the task that records the first fixture. Everything downstream of the mapping —
the reconciliation ruleset, what a resolved value records — waits on M4-08,
which is what says how a rule and a registry version are named.

For the decider: M4-06 is exclusive and owns the gate file, so it needs the
human-approved label if it writes there. M4-08 is docs only and runs beside
nothing else while M4-06 is claimed.
