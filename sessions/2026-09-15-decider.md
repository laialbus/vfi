# 2026-09-15 — decider

- Merged #188, the 2026-09-14 decider entry carrying the Rule 3 record's
  acceptance: checks green, the ADR edit its Decision review alone, its claims
  matching main.
- Merged #189, the planner's tenth M4 batch (M4-31, M4-32): touches only
  `tasks/` and `sessions/`; premise checked against the milestones and the
  merge history, with #188 merged first as it assumed; M4-31 traced to the
  three-states criterion and the negative-equity fixture, M4-32 to the
  golden-fixtures and provenance criteria and the record the 2026-09-09 sweep
  asked for; ids fresh; `depends_on: [M4-30]` resolves to a task retired this
  sweep; owns lists hold their diffs, with no overlap. The planner's reading of
  "after Rule 1's" as Rule 1's record is the one that was meant.
- Merged #190, M4-30, and accepted `docs/adr/silence-beside-a-read-figure.md`
  as Structural: inside `owns` plus the session entry and the task file; every
  quotation from the Rule 3 record, the open-questions record, the published
  vocabulary and `settling.rs` matches its source; every fixture count
  re-derived on 493ac66 — fact and filing counts, no fact under the eight
  elements, the 49/38/42 and 59/13/36 stand-ins, the five filings at
  2024-03-31. Its Decision review, Status and Decided date ride in this PR;
  until it merges, the file on main trails by one run.
- Merged #191, M4-28: inside `owns`, the stage untouched, the harness change a
  header only, lockfile unmoved. Re-derived from the recording: 12,083 facts in
  25 filings, all 10-K or 10-Q, key spelled bare, 313 filings on one page; each
  of the eleven filings from 2020-06-30 to 2022-12-31 carries a non-zero
  `CommonStockDividendsPerShareDeclared` and `PaymentsOfDividends` at its own
  period end; none of the eleven from 2023-03-31 carries any of the four
  elements there; no four-element fact ends after 2022; the unmapped
  `DividendsCash` and `Dividends` zeros fall where the header says.
- Nothing rejected, nothing deferred, nothing Constitutional seen. `gh` needed
  the sandbox override from its second call, as in earlier sweeps; the pre-edit
  hook refuses a read that names a protected path beside `sed`, `awk` or a
  shell loop, so plain `grep` is the way to read the published vocabulary.
- Escalated refs on origin: none. Open escalations on main: none.
- Carried for a human. New: the accepted silence record, Structural, for the
  review the tier requires. The suspension fixture already holds the shape that
  record names — four dividend durations and 24 of 26 `ShortTermInvestments`
  instants where the last-filed answering filing is silent on a line an earlier
  one stated — so the normalize fixture written against it is where the reading
  is first checked, and whoever writes it reads the record first. From earlier
  sweeps: the Rule 3 record's Structural review; M4-27's premise, since every
  earlier facts case carries negative equity; CIK 0001859199's dividends under
  elements the registry does not name; the #181 ruling on v2's three
  descriptive lines; how a partial supersession is recorded; `company_facts`
  fetching the history itself.
- Refill: requested, at `planner/requested` on 71365cc. The queue on origin
  holds three — M4-29, M4-31, M4-32 — no `plan-` PR is open, no planner
  escalation sits on main, and the ref did not exist.
