# 2026-09-01 — M4-09

- Branch `M4-09`, PR: creates `registry/` with the concept half of the tag
  mapping — 28 files, 75 rules — and the `registry` gate over it. `docs/layout.md`
  names the directory and both its halves, so the per-filer half needs no second
  amendment.
- Transcribed from `docs/adr/tag-concept-registry.md`, element for element.
  Every exclusion the record writes down is absent, and sits in its concept file
  as a line saying it is not that concept — the trap is where the next editor is.
- The gate reads the concepts and kinds out of the highest version the published
  vocabulary's own record names, never a copy, so a twenty-ninth concept turns it
  red until accounted for. A rule id is rendered from its concept, its sorted
  kind scope, its form and its operands; nothing writes one down.
- Two refusals past the ones the task enumerates, both the record's own rules
  about this half: an assertion in the general mapping, and a concept accounted
  for both ways. Twenty-two in-band cases, one of them clean.
- `reading` and `answers` are not written. `docs/adr/candidate-choice.md` is
  still Proposed, so the gate refuses them as undeclared fields until the task
  that adds them lands.
- Task file deleted. Gates green.
