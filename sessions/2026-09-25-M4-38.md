# 2026-09-25 — M4-38

Branch `M4-38`. `history::history` runs one filer's facts (a fetch `Filer`)
and the registry into what `canonical-concepts` v2 publishes: the filer, Rule
1's periods, and every concept at each, crossed from what `standing::stands`
answers. The kind comes from `Registry::kind_of`. At a tie, each tied filing's
attempt now carries what candidate choice declined inside it (settling keeps
it). An unreadable `filed` makes the surface fail with the accessions, whether
Rule 1 met it or the run did. `normalize()`, its golden harness and the
benchmark are untouched.

For the decider: every pinned count re-derives (32 periods, 269 read, 51
silence, 576 `Unknown`, 422 over several filings, no assertion or
`NotApplicable`). A filing Rule 3 left behind on `form` under a tie is not
carried: carrying it would break the ruling's "an accession the tie does not
name came to nothing".
