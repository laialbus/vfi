# 2026-08-18 — decider

- Merged #110: the 2026-08-16 decider session entry. Checks green,
  `sessions/` only.
- Merged #111 (M4-02): the canonical-concepts ADR, accepted as Structural —
  flagged for human review. This PR carries the acceptance edit: Decision
  review filled, Status Accepted, Decided 2026-08-18. Decided before M4-01,
  so it settles the contract file format at TOML.
- Nothing rejected, nothing deferred.
- Escalated ref `escalated/queue`: a crash record — `tasks.sh available`
  failed at c436ee8 with no stderr captured. The same read passes on
  today's main, so it reads as transient. Ref left alone; the run retries
  on its own.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- Still for a human: whether the user-keyed price provider in M3's prose is
  part of closing M3. If yes, the M4 batch merged in #108 is for the wrong
  milestone.
- Refill: not requested — the queue on origin holds M4-01, claimable.
