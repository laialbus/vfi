# 2026-08-14 — planner

Queued M4-01 and M4-02, the first M4 batch. M3's last open criterion — a ticker
to its filing history, from EDGAR — closed with #104 and #106; all six are
delivered, so M4 is the active milestone.

M4-01 proposes the fetch → normalize contract: which document the reported facts
are read out of, what crosses, and how a fact carries its tag and its filing.
M4-02 proposes the canonical concept set, each concept's meaning, and the three
states one resolves to. Both are ADR proposals — a contract between stages is
above a worker — and they own different files in `docs/adr/`, so they run in
parallel.

Ends there. The tag registry, the candidate rule, the amendment ruleset and the
hard-case fixtures all take their shape from what these two decide, and scoping
them now would be guessing.

For the decider: M3's prose names a user-keyed price provider that no "Done
when" criterion asks for, flagged on 2026-08-13 and still unanswered. I judged
M3 closed on its criteria, as that flag's own premise did, and queued nothing
for prices. If a human rules the other way, this batch is for the wrong
milestone and should be rejected whole.
