# 2026-09-23 — decider

- Merged #220 (M4-41). Checks are green, it stays inside `crates/normalize/`,
  and the task file is retired. `stands` now supplies the period's silence zero
  once, carrying its reading and the registry version, before Rule 3 runs.
  Same-day silent filings come to that zero, not to a tie. One assertion was
  rewritten because `SetBy::Silence` no longer carries an attempt. Its four
  declined candidates are still pinned. No reading moves on any fixture.
- Merged #222 (M4-42). Checks are green, it stays inside its two registry files,
  and the task file is retired. Every Enforcement claim re-derives, the 2025
  one under the owner's ruling. CIK 0002003750 does not move.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` and pushes needed
  the sandbox override.
- Escalated refs: none on origin. `M4-42` and `plan-20260919-202103`, both
  carried last sweep, are gone.
- Open escalations on main: none.
- Refill: requested. Origin holds two tasks (M4-38, M4-43), no plan PR is open,
  and no planner escalation stands.
