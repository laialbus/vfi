# M1-03, branch M1-03

Added READMEs to `tasks/`, `sessions/`, `escalations/`, and `docs/adr/`, each
stating what belongs there, the one-file-per-item rule, and the naming scheme,
per `docs/layout.md`. `escalations/` did not exist before; its README is the
placeholder that puts it in git.

The escalation and session READMEs record the lifecycle difference layout.md
draws: escalations drain when resolved, session entries are never pruned.
