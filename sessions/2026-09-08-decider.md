# 2026-09-08 — decider

- Merged #159, the 2026-09-07 decider entry: checks green, a session entry
  alone, its claims matching main.
- Merged #160 (M4-21): checks green; one file under `registry/filers/` plus
  the session entry and the task file's deletion. Re-derived from the merged
  fixture: every subtotal the file cites reconciles — 76,485 + 5,922 = 82,407;
  743,372 + 2,803 + 6,159 = 752,334; 804,887 − 439,260 = 365,627;
  365,627 − 388,580 = −22,953 — and none of the filer's 117 elements is an
  interest-income, deposit, premium, claim, reserve, fair-value, real-estate
  or net-investment-income one. `cik` and `kind` only; the registry gate read
  it green.
- Merged #161 (M4-20): checks green; a case directory under `fixtures/fetch/`,
  a header comment in the fetch harness, the session entry, the task file's
  deletion, and one escalation file. Re-derived from the recording: 4,232
  facts, no five-field collision, a 10-KT carrying 2023-05-01 to 2023-12-31 at
  245 days, the years 2022-05-01 to 2023-04-30 and 2024-01-01 to 2024-12-31
  either side, quarters May–Jul and Aug–Oct before and Jan–Mar, Apr–Jun,
  Jul–Sep after. The lockfile and the first case unmoved. The escalation file
  rides outside the owns list the way a session entry does, as #25, #30, #38
  and #78 carried theirs.
- Merged #162 (M4-17): checks green; diff inside `crates/normalize/` plus the
  session entry and the task file's deletion; lockfile unmoved, `normalize`
  untouched. Every test reads the merged fixture and reaches its rule through
  the registry interface; the sweep tallies each fact under the two swept
  elements answering exactly once. Two flagged shapes stand: a balance is
  answered only at an instant, never at a duration's end — Rule 1 of the
  alignment record makes every instant a period of its own, so nothing is
  unreachable and the loose reading would key one figure twice; and a unit key
  is a currency when it is three uppercase letters, ISO 4217's shape, with no
  copy of ISO's list. One seam for M4-19 to watch: under
  `answers = "filing_reported_in"` the cover fact answers whatever period is
  asked, a duration included, while the strict reading refuses a balance at a
  duration otherwise. M4-19's acceptance never admits that entry at v1, so
  nothing reaches it today.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` needed the sandbox
  override after its first call, as before.
- No escalated ref on origin; the M4-13 crash record noted last sweep has been
  swept.
- Open escalations on main, one: `2026-09-08-companyfacts-cik-as-a-number.md`.
  Fetch refuses a companyfacts document whose `cik` is a bare number — 207 of
  the 217 filers the worker sampled — so `company_facts` fails for most filers
  and both facts fixtures are recordings of the minority. Recommend:
  Structural. The contract's `cik` field reads "as the document states it",
  published as-is, while the registry record keys a filer by the ten-digit
  padded CIK, so the two spellings cannot both cross as published. A record
  fixing `cik` as the padded key — folded into the `fetch-normalize` v2 the
  alignment record already proposes for `report_period_end`, so one version
  carries both — is decider-acceptable; the fetch task that reads both
  spellings and records a number-spelled filer follows it. The file names no
  task, so it parks nothing.
- Carried for a human from the two sweeps before: the accepted alignment
  record states Rule 3 two ways — its Decision names the greatest-`filed`
  filing for every agreeing value, its restatement fixture expects agreeing
  values to keep naming the earlier one. Recommend the next plan queue a task
  proposing a superseding record for Rule 3 alone, Structural, ahead of
  whatever implements it. M4-19 stops before Rule 3, so nothing queued depends
  on it yet.
- Refill: requested, `planner/requested` at 92c42b8. The queue holds two —
  M4-18, M4-19 — below the line; no `plan-` PR is open, no planner escalation
  sits on main, and the ref did not exist.
