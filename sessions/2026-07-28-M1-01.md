# M1-01 — Define and document the workspace layout

- **Branch:** `M1-01`
- **Task:** M1-01 (file deleted in this diff)

Added `docs/layout.md`: the map from artifact kind to home, covering the repo,
the workspace around it, and everything M0 created. It fixes the crate naming
convention (`crates/<stage>/` → `vfi-<stage>` → `vfi_<stage>`), names `shell/`
as the Python path, and states the one-file-per-item rule with the naming
scheme for `tasks/`, `sessions/`, `escalations/`, and `docs/adr/`.

Rewrote `README.md` to point at CLAUDE.md and docs/layout.md.

`contracts/` and `scripts/gates.sh` are recorded at the paths the protected-path
list already fixes; this document does not move them.
