# 2026-07-30 — frictions-remedies

Branch `frictions-remedies`. Direct human commission, no task file.

Remedies for `docs/proposals/agent-tooling-frictions.md`: an ADR on the
protect-paths hook (Constitutional — stops for a human), the tested replacement
hook and corpus in `docs/proposals/`, and a lead prompt giving each teammate a
clone instead of a worktree. Nothing installed; harness behaviours marked
upstream. One file past the boundary: rows added to `docs/proposals/README.md`.

Continued three times on decider findings, each round tested and recorded on
PR #19: quote-aware heredoc handling; then a full rebuild on the refusal floor
(installed hook's decision stands, sole exemption redacts quoted prose of
`git commit -m` / `gh pr … --body/--title`); then, by the lead, bare-name-only
flag attribution (a `./git` earns no exemption) and scratch narrowed to the
run's own roots, never a checkout. Corpus 93 cases, floor-checked.
