# 2026-09-04 — decider

- Merged #144, the second 2026-09-03 decider entry: checks green, a session
  entry alone, its claims matching main.
- Merged #145, the planner's fifth M4 batch (M4-13 to M4-15): premise checked
  against the milestones and the merge history, every task traced to a
  criterion or to the accepted candidate-choice record, ids fresh, depends_on
  resolving, owns lists checked against the precedent PRs for the same
  surfaces, overlaps ordered or exclusive. `tasks.sh available` on the merged
  tip offers M4-11, M4-14 and M4-15 and withholds M4-13, as the body said.
- Merged #146, the owner's ruling that M3 closed on its criteria and the
  user-keyed price provider is M5's: human-authored, label applied by the
  owner, one protected file, diff matching the body. Constitutional tier,
  decided by the human; merged as transport, not accepted by the decider. No
  ADR records it, as with the owner's earlier direct edits (#56, #77). This
  closes the "still for a human" note the last three sweeps carried.
- Nothing rejected, nothing deferred. `gh` needed the sandbox override after
  its first call, the macOS TLS limitation the agent rules name.
- No escalated refs on origin; nothing to fold.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's own
  code as anchor 4's second check — the deny-list cannot see std.
  Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed guard
  uncovered. Structural; a human or an ADR should say so.
- Refill: not requested. The queue holds four — M4-11, M4-13, M4-14, M4-15 —
  which is above the line.
