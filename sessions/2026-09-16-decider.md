# 2026-09-16 — decider

- Merged #192, the 2026-09-15 decider entry accepting the silence record:
  checks green, and its claims match main.
- Merged #193, the planner's eleventh M4 batch (M4-33). Its premise holds: M4-28
  is merged and #192 went in first. It traces to the three-states criterion and
  the suspension fixture. The id is fresh, `depends_on` is empty, and the owns
  list is one file with no overlap. The recording has no fact under the seven
  elements the batch names.
- Rejected #194, M4-29, and deleted its branch. The recording and nearly all of
  the `reit` header re-derive, but two lines do not. Real estate at cost less
  accumulated depreciation is net at 42 of 43 instants, not "every": at
  2011-12-31 cost is 11,500,000. And `OperatingLeasesIncomeStatementMinimumLeaseRevenue`
  stops at the Q1 2018 10-Q, not the 2018 10-K. Resubmit on a fresh claim; the
  work is at `refs/pull/194/head`.
- Merged #195, M4-32, and accepted `docs/adr/what-normalize-emits.md` as
  Structural. The quotations match, and all thirteen fixture claims were
  re-derived by a throwaway run of the landed procedure. Its Decision review,
  Status and Decided date ride in this PR; until it merges, the file on main
  trails by one run.
- Nothing deferred, nothing Constitutional seen. `gh` needed the sandbox
  override. The pre-edit hook refuses a heredoc that names the published
  vocabulary's directory, so PR comments went through `--body-file` from
  `target/`.
- Escalated refs on origin: none. Open escalations on main: none.
- Carried for a human. New:
  - the accepted emit record, Structural;
  - its wrong-shape silence zeros (45 of 96 on CIK 0002003750), removable
    before storage only by a record superseding the Rule 3 table rows;
  - the conditional dividend zeros in Q4 2020 and Q4 2021 on CIK 0001778784,
    though the filer paid.

  From earlier sweeps:
  - the Rule 3 and silence records' Structural reviews;
  - M4-27's premise;
  - dividends under unmapped elements on 0001859199 and 0001778784;
  - the #181 ruling on v2's descriptive lines;
  - how a partial supersession is recorded;
  - `company_facts` fetching the history itself.
- Refill: requested, at `planner/requested` on 08dd910. The queue on origin
  holds three (M4-29, M4-31, M4-33), no `plan-` PR is open, no planner
  escalation sits on main, and the ref did not exist.
