# 2026-08-06 — M2-11

Branch `M2-11`. `scripts/gates.sh` now says what every gate, proof, sweep, and
run costs, on every form, and the two multipliers behind the cost are gone: a
proof runs the one gate its violation belongs to instead of the whole sweep,
and the corpus gives each case its own copy of a template repository so the
cases run at once instead of taking turns resetting one.

Measured on this machine, before and after, whole suite: 4m07s → 49s. The same
ten proofs catch, the same 58 cases are pinned, and against a tampered
`tasks.sh` both runners report the same cases red with the same complaint — 17
under the proof's own tamper, 58 under a broad one.

`scripts/gates.sh` is protected and this task owns it, so the pull request
needs the `human-approved` label.
