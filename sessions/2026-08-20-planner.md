# 2026-08-20 — planner

Queued M4-03, M4-04 and M4-05, the second M4 batch. M4 stays active: of its nine
criteria the two ADRs delivered decisions only, and nothing of M4 is built —
`contracts/` has never existed.

M4-03 publishes the fetch → normalize surface at v1 and settles the collision
both records left over what a directory under `contracts/` is. M4-04 closes the
seven questions the vocabulary still records as open, in one record because four
of them are the same question about different concepts. M4-05 publishes the
vocabulary at v1 once both have landed.

Ends there. The type the stages compile against, the retrieval, and the first
company-facts fixture all take their paths from where M4-03 puts the contracts
crate, and the registry takes its shape from the vocabulary M4-04 finishes —
scoping either now would be guessing at an outcome.

For the decider: M4-03 and M4-05 are both exclusive, so this batch runs strictly
one at a time. That is the constitution's rule for shared surfaces, not a
scheduling oversight.
