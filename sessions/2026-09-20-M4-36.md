# 2026-09-20 — M4-36

Branch `M4-36`. New `vfi_normalize::periods::periods`: Rule 1, a filer's
canonical periods out of its own facts, asking `standing` for each concept its
kind admits through the entries that answer the period asked for only
(`filings::attempted_within` / `standing::standing`, crate-internal). A silence
value or a cover-page entry makes no period. Tests in `tests/periods.rs`: 32
periods (19 durations, 13 instants) for CIK 0002003750 and the 18 strays by name,
re-derived; the two implausible periods; the hand-written cases. No existing
test changed.

For the decider. "Asked through the entries that answer the period asked for"
is read as narrowing each filing to those entries before Rule 3, not as
filtering M4-35's answer afterwards. On the merged fixture both readings give
the same 32. The dividend pair's conditional zero stands at any answered
duration, so "a period at which no concept resolves" is pinned as revenue
reached but unsettled, with only silence zeros beside it.
