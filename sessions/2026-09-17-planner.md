# 2026-09-17 — planner

Queued M4-34, M4-35, M4-36 and M4-37, the twelfth M4 batch, on the decider's
refill request; the queue on origin held no task at all. M4 stays active:
nothing runs through the stage end to end, the normalize golden fixture still
pins the stage doing nothing, and five of the six fixtures the milestone names
are unwritten.

M4-34 asserts the kind of the no-gross-profit filer M4-29 recorded, CIK
0002033615 — the last filer a named golden fixture is written over that has
none — serving the three-states criterion. M4-35 and M4-36 put the two rules
`lib.rs` says the crate does not carry into code: Rule 3 with the silence
reading beside it, then Rule 1 over it, serving the stated-ruleset and
every-value-records-its-source criteria. M4-37 publishes the vocabulary at v2
as the accepted emit record states it, which is what the wiring writes into.
M4-36 follows M4-35 because Rule 1 asks the settled answer; M4-37 is exclusive
and so never runs beside either.

Ends before the wiring and the six normalize fixtures. Both wait on all three
of the above landing, and the fixtures wait on more than that: the emit record's
suspension-fixture claims were re-derived while CIK 0001778784 had no kind, and
M4-33 has since asserted `bank`, which makes `short_term_investments`
`NotApplicable` for that filer at every period. That is carried for the decider,
not queued. No fetch recording is queued either — the accepted Rule 3 record
places the tag-change case on the fiscal-year-change fixture already in the
tree. CIK 0001715819 still has no kind and no fixture assigned, and nothing
here gives it either.
