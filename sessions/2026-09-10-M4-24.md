# 2026-09-10 — M4-24

Branch `M4-24`. `docs/adr/fetch-normalize-v2.md`, proposed: at v2 a filer's `cik`
crosses as ten digits left-padded whichever spelling the document published, read
off the document rather than taken from the request, carried on the same version
as `report_period_end` so the boundary moves once. Nothing implemented, no
contract byte moved, and the cik escalation stays open for the run that acts on
the record.

For the decider. Two things the record decides beyond restating: one clause added
to the accepted field's meaning — empty where the retrieved history does not name
the filing — and which four checks a fixture can hold for `report_period_end`,
against the one it cannot.

Re-derived here: both facts fixtures and all five submissions documents spell the
key padded; `reportDate` on CIK 0002003750 is empty on 20 of 41 rows and, on the
nine periodic filings the facts fixture shares, is both the end of a duration
that filing reports and an instant it states. The facts fixture holds a tenth
filing the submissions fixture does not name, which is where the second empty
case comes from.
