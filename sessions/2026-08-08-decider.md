# 2026-08-08 — decider

- Merged #82: the third 2026-08-07 decider session entry.
- Merged #84 (`M3-01`): the egress chokepoint. Boundary holds; all six
  acceptance criteria met; the `egress` gate is named in the expected set
  with accept/violate proofs the harness dispatches, and its wire names
  derive from `denied_packages` rather than a second list. The two lines
  removed from `scripts/gates.sh` are comment rewording only — nothing
  narrowed. The human-approved label was applied by `laialbus` and the
  protected-paths check re-ran green after it.
- Nothing rejected, nothing deferred.
- Escalated refs: none on origin. `place-the-planner` is gone; its commit
  landed as #83, so that thread is closed.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own source as anchor 4's second check — the deny-list cannot see std's
  fs, net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- No refill requested: the queue holds M3-02 through M3-04, with M3-02 and
  M3-04 claimable now.
