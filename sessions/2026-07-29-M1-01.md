# M1-01, branch M1-01

Redo of the run rejected as PR #6. Started from that work (`e00f4d9`) rather
than rewriting: fixed the two defects the decider named in `docs/layout.md` —
`.claude/rules/` was wrongly marked protected, and `.gitignore` had no home —
and added a note that the protected-paths list, not this document, is what makes
a path protected.

Also folded in the signed-off amendment from `docs/proposals/escalation-lifecycle.md`
(PR #8) and deleted that proposal, since an installed draft left in place is a
second copy of the layout text.
