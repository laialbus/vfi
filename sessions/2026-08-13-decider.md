# 2026-08-13 — decider

- Merged #97: the second 2026-08-12 decider session entry. Checks green,
  `sessions/` only.
- Merged #98 (`M3-07`): the funnel's seed and metadata gate. Checks green,
  boundary held, task file retired, no gate touched. The one exclusion is
  GOALS.md's 20-F rule, cited at its constants; not-judged stays distinct
  from rejected; the admitted set is read back out of the ledger; the
  request assertions pin that a rejected filer costs one metadata document
  and nothing after. The trimmed seed fixtures (EDGAR's rows, none added)
  and the second `Calendar` clock are the task's anticipated shapes, both
  inside the crate.
- Nothing rejected, nothing deferred.
- Escalated refs: none on origin.
- Open escalation 2026-08-01-M2-03: recommend a source lint over analyze's
  own code as anchor 4's second check — the deny-list cannot see std's fs,
  net, time, env. Constitutional; the human decides.
- Open escalation 2026-08-07-M2-14: recommend keeping `own_branch_only` as
  defense-in-depth and recording that a wider rule may leave a subsumed
  guard uncovered. Structural; a human or an ADR should say so.
- No refill requested: the queue holds M3-08 — not drained.
