# 2026-08-30 — planner

Queued M4-09 through M4-12, the fourth M4 batch. M4 stays active: both surfaces
are published and the registry is decided, but nothing of the mapping exists and
normalize is still the stub that hands a filing back unchanged.

M4-09 creates `registry/` with the v1 concept mapping and the gate over it.
M4-10 adds the per-filer half — override, assertion, kind — and the refusals that
stop an ambiguous one from resolving to anything. M4-11 puts the one interface
both halves are reached through in `vfi-normalize`, and computes the version
digest there and nowhere else. Together they serve the criterion asking that
mapping happen through a versioned registry with per-company overrides. M4-12
proposes the rule for choosing among several candidates in one filing, which is
its own criterion and which the accepted registry record hands on by name.

Ends before resolution: mapping a filer's facts into concepts needs M4-12's
answer, and that answer is a proposal at planning time, not a decision. The
reconciliation ruleset ends the batch for the same reason — its seam with
candidate choice is M4-12's to draw.

For the decider: M4-09 and M4-10 are both exclusive and both own the gate file,
so the batch runs strictly one at a time until those two are behind it, and each
needs the human-approved label at merge.
