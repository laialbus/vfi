# 2026-09-18 — decider

- Merged #202, the second 2026-09-17 decider entry. Checks green; sessions only.
- Merged #203 (M4-35, `standing::stands`: Rule 3 and the per-period silence
  reading). In boundary, no existing test touched, both restatement tables and
  the `preferred_equity` claim pinned against the fixtures. Flagged for a human,
  before M4-36 builds on it: two same-day filings that are both silent come to
  `Unknown` under Rule 3's tie clause. The silence record reads that case as the
  zero, so the two accepted records disagree at this edge (Structural). The two
  other gap-fills (`None` where no filing answers, `Undecided::Undated`) lean
  toward absence.
- Merged #204 (M4-34, CIK 0002033615 `kind = "bank"`). Figures spot-checked
  against the filings they cite; sums close.
- Nothing rejected, nothing deferred, no ADR accepted. `gh` and pushes needed
  the sandbox override.
- Escalated refs on origin: none. Open escalations on main: none. The
  dividend-suspension fixture staleness in the emit record, flagged 2026-09-17,
  still waits on a human.
- Refill: requested (`planner/requested` at 8c80b56). Two tasks are left on
  origin/main (M4-36, M4-37). No plan PR is open and no planner escalation.
