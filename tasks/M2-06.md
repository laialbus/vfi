---
id: M2-06
title: Build the benchmark harness with a committed baseline and threshold
milestone: M2
owns:
  - scripts/gates.sh
  - crates/
depends_on: [M2-05]
exclusive: yes
acceptance:
  - A benchmark harness exists with a committed baseline and a set regression
    threshold. The threshold is data, defined in one place, with its reason
    stated beside it.
  - The gate fails when a benchmark regresses past the threshold against the
    committed baseline.
  - A green run on an unchanged tree is repeatable — the gate distinguishes a
    regression from machine noise and does not flake in CI.
  - Proof-of-catch, a scratch copy with a deliberate slowdown injected turns
    the single command red, running with the gates in CI.
  - The gate runs from scripts/gates.sh and CI runs the same command.
---

The baseline's home is a new kind of artifact; give it its place in
docs/layout.md in the same diff if none of the existing homes fits.
