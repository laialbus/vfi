---
id: M4-47
title: Record the golden fixture for the filer whose shareholders' equity is negative
milestone: M4
owns:
  - fixtures/normalize/a-filer-whose-shareholders-equity-is-negative/
depends_on: [M4-44]
exclusive: no
acceptance:
  - '`fixtures/normalize/a-filer-whose-shareholders-equity-is-negative` holds
    CIK 0002011954, read from the merged fetch fixture of that name. It uses
    the input form, the claims form and the rendering M4-44 fixed, and adds
    nothing outside its own directory.'
  - '`claims` is hand-derived from that fixture''s facts, and each claim names
    the facts it comes from. It meets the floor
    `docs/adr/golden-claims-and-baseline-re-recording.md` sets for a filer no
    accepted record pins a claim for: the period set with its count, every
    line of one instant and one duration in full, and the negative equity,
    which is the fixture''s reason to exist. The negative equity claim covers
    each admitted instant where `shareholders_equity` resolves below zero, with
    its figure, its source tag and its accession, and at least one of the two
    periods in full is such an instant.'
  - '`expected` is recorded from the engine only once every claim holds
    against it, and the pull request says so. Where the facts show no negative
    equity the stage resolves, or a claim does not re-derive, the run stops and
    escalates rather than restating it.'
  - 'Nothing proved gets narrower. Every other fixture still produces its
    expected result, and the crate, harness, registry, contracts and benchmark
    are untouched. No gate is added or weakened.'
---

One of the golden fixtures GOALS.md's M4 names, negative equity. No accepted
record pins a claim about this filer, so the golden-claims record's floor is
the whole of what the claims must hold, derived from the facts by hand.
