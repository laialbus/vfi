---
id: M1-02
title: Create the Rust workspace, one crate per stage
milestone: M1
owns:
  - Cargo.toml
  - Cargo.lock
  - crates/
  - .gitignore
depends_on: [M1-01]
exclusive: no
acceptance:
  - The workspace compiles locally (cargo build) with one empty crate per
    pipeline stage — fetch, normalize, analyze, store — named per the
    convention in docs/layout.md.
  - No crate depends on another yet; edges are added only by later tasks.
  - Build artifacts (target/) are ignored by git.
---

Empty is fine. The dependency edges, contracts, and the analyze deny-list are
later milestones' work — do not anticipate them. CI's gates job stays a stub
until M2; do not touch .github/ (protected). See ANCHORS.md anchor 2 for why
one crate per stage.
