---
id: M4-45
title: Record the golden fixture for the filer that changed its fiscal year and its tags
milestone: M4
owns:
  - fixtures/normalize/a-filer-that-changed-its-fiscal-year/
depends_on: [M4-44]
exclusive: no
acceptance:
  - '`fixtures/normalize/a-filer-that-changed-its-fiscal-year` holds CIK
    0001859199, read from the merged fetch fixture of that name. It uses the
    input form, the claims form and the rendering M4-44 fixed, and adds
    nothing outside its own directory.'
  - '`claims` is hand-derived, and each claim names the record or the facts it
    comes from. It states the period set with its count and every line of one
    instant and one duration in full. It also holds every claim the accepted
    records make about this filer as they now stand, the
    `docs/adr/README.md` table included. In
    `docs/adr/period-alignment.md`, the transition period is present at its
    own two dates, the years either side are present at theirs, no period is
    synthesised between them, and the quarters on the two calendars do not
    collide. In `docs/adr/which-filing-sets-the-value.md`, the transition
    period 2023-05-01 to 2023-12-31 carries its six values, each naming the
    10-K/A filed 2025-05-13. At the six periods it names, the source tag moves
    while the figure does not, and in the 2026 filings
    `EarningsPerShareDiluted` returns.'
  - 'Also from the records: in `docs/adr/what-normalize-emits.md`, at the
    instant 2024-12-31 `preferred_equity` is a `read` 0 naming
    `0002008589-25-000033`, and `short_term_investments` is a `silence` 0. In
    `docs/adr/declared-cash-dividends-stand-in.md`, the year 2025 reads
    `preferred_dividends` 122,877 from `DividendsPreferredStock` in
    `0001213900-26-026653`.'
  - 'This one fixture serves two of the cases the milestone names: the fiscal
    year change, and the company that changed tags mid-history, which the Rule
    3 record says this filer already holds. `claims` says which of its claims
    serve which case.'
  - '`expected` is recorded from the engine only once every claim holds
    against it, and the pull request says so. Where a claim does not
    re-derive, or the fixture needs a rule no record states, the run stops and
    escalates rather than restating the claim or adding the rule.'
  - 'Nothing proved gets narrower. Every other fixture still produces its
    expected result, and the crate, harness, registry, contracts and benchmark
    are untouched. No gate is added or weakened.'
---

One of the golden fixtures GOALS.md's M4 names: "a fiscal-year change, a
company that changed tags mid-history". The accepted records already say what
this filer's fixture must show, and `golden-claims-and-baseline-re-recording.md`
says how it is written: hand-derived claims beside a recorded rendering.

Unordered against M4-46 to M4-48, since each owns only its own fixture
directory.
