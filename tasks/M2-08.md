---
id: M2-08
title: Prove a slow engine job keeps its caller responsive
milestone: M2
owns:
  - crates/
  - docs/layout.md
depends_on: [M2-07]
exclusive: no
acceptance:
  - The engine exposes a deliberately slow job — long-running by design — in a
    form that does not block its caller.
  - An automated test proves it, the caller does observable work while the job
    runs and still receives the job's result. The test runs with the ordinary
    test suite, so it is already inside the gates.
  - No GUI is built and no toolkit is chosen — that is ADR-001 and stays open.
    Everything in this task lives in the engine and its tests.
  - If a non-stage crate is added for the job, it follows the crate naming
    rules and docs/layout.md is amended in the same diff.
---

This is M2's last criterion in GOALS.md. The interface that must stay
responsive does not exist yet, so what this task proves is the seam: the
engine's shape lets a slow job and a live caller coexist. If proving that
honestly seems to require building any part of the shell, stop and escalate —
that would be a scope question for a human, not a judgment call.
