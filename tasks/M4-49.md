---
id: M4-49
title: Park a task on its escalated/ ref, not only on the file the sweep later lands
milestone: M4
owns:
  - scripts/tasks.sh
  - scripts/tests/tasks.sh
depends_on: []
exclusive: no
acceptance:
  - '`scripts/tasks.sh available` does not offer a task for which a ref
    `refs/heads/escalated/<id>-*` exists on origin, as the accepted
    `docs/adr/escalated-refs-park-their-task.md` rules, the id matched whole
    against the queue''s own ids as `parked` already matches a file name. The
    refs are read from the one `ls-remote` that already reads the claims; no
    second read of origin is made.'
  - 'The file on main parks a task exactly as before, with or without a ref.
    A ref alone parks it; deleting the ref with no file on main offers the task
    again. The corpus in `scripts/tests/tasks.sh` gains one case for each of
    the three, in the shape the existing cases take.'
  - '`check` does not change, so the queue gate and its proof of catch do not
    move. `claim`, `release` and the sweep''s subcommands are untouched except
    where they already read the same branch list.'
  - The gate runner is not edited, no other script moves, and no gate is added
    or weakened.
---

Three runs took M4-38 in one day and stopped on the same wall, because a stop
becomes visible to `available` only once a sweep lands its file on main. The
owner's record closes the gap from the tool's side; the sweep's duty to delete
a folded ref is the decider's and is stated in the record, not here.

This task advances M4 by keeping its queue from handing a parked task out; it
adds no product code. Not in this diff: any change to the gate runner, the
worker wrapper, or the decider's prompt.
