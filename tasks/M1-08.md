---
id: M1-08
title: Drain the installed proposals
milestone: M1
owns:
  - docs/proposals/
depends_on: []
exclusive: no
acceptance:
  - docs/proposals/protect_paths.py, docs/proposals/lead-prompt.md, and
    docs/proposals/agent-tooling-frictions.md are deleted.
  - Before deleting the hook draft, a read confirms the installed copy exists
    at its destination named in the proposals README. The lead prompt's
    destination is outside the workspace; the human's 2026-07-30 confirmation
    recorded here is the authority — do not write outside the workspace to
    check it.
  - The README table lists exactly what remains — the two launchd plists and
    protect_paths_tests.py — with their install conditions.
  - Nothing outside docs/proposals/ changes.
---

The hook was installed and its ADR accepted in PR #20; the human confirms the
lead prompt is installed (2026-07-30). A draft kept after installation is a
second copy of a live file, which the one-source-of-truth rule bans. The
frictions doc drains on the same event by its own terms — its remedies are
installed, and its upstream vendor items remain recoverable in git history and
PR #19. The tests file stays: it was not installed, so the draft is still the
only copy.
