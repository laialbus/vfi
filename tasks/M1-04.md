---
id: M1-04
title: Create the operational scripts
milestone: M1
owns:
  - scripts/
depends_on: [M1-01]
exclusive: no
acceptance:
  - scripts/tasks.sh available prints the ids of claimable tasks by reading
    tasks/ frontmatter and git (dependencies merged, branch absent on origin),
    maintaining no list of its own.
  - scripts/ci-status.sh <branch> exits 0 iff the latest CI run on that branch
    is green (via gh).
  - scripts/escalate.sh <task-id> <reason> writes one escalation file into
    escalations/, per that directory's naming scheme, creating the directory
    if needed.
  - Each script is executable, has a one-line usage comment, and works on
    macOS bash 3.2.
  - No script is added at the repo root or anywhere outside scripts/.
---

Do NOT create scripts/gates.sh — it is a protected path and arrives in M2
through the proposals flow with human sign-off. tasks.sh is what run.sh and
the lead use to see the queue; keep its output to bare ids, one per line.
