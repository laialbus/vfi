---
id: M4-42
title: Name the equity statement's declared-cash dividend elements in the registry as stand-ins
milestone: M4
owns:
  - registry/concepts/dividends_paid.toml
  - registry/concepts/preferred_dividends.toml
depends_on: []
exclusive: no
acceptance:
  - 'Three `tag` entries with reading `stand_in`, as the accepted
    `docs/adr/declared-cash-dividends-stand-in.md` states them:
    `us-gaap:DividendsCash` and `us-gaap:DividendsCommonStockCash` under
    `dividends_paid`, and `us-gaap:DividendsPreferredStock` under
    `preferred_dividends`. Each file''s header states, in a sentence, why the
    declared line stands in for the concept and what it leaves out, and the
    `dividends_paid` header adds the record''s exclusion: `Dividends` is not
    this concept.'
  - 'The registry gate reads green. The registry version changes, as it does on
    every entry added, and nothing in the tree pins the previous digest; where
    something does, the run stops and escalates rather than updating it.'
  - 'Re-derived by a throwaway run through the crate''s own surface, over the
    two fetch fixtures that carry these elements, and stated in the pull
    request: every claim in the record''s Enforcement section — the quarter
    2020-04-01 to 2020-06-30, the years 2022, 2023, 2024 and 2025 and the
    quarter 2020-10-01 to 2020-12-31 on CIK 0001778784; the year 2025 and
    `dividends_paid` at every period on CIK 0001859199. Where a claim does not
    re-derive, the run stops and escalates rather than adjusting it.'
  - 'The counts M4-38 pins on CIK 0002003750 do not move: that fixture carries
    no fact under any of the three elements, and the run states that it checked.'
  - Nothing else moves. No other registry file, no filer file, no contract
    byte, no crate, no fixture and no test changes; no dependency is added so
    the workspace lockfile does not move; and no gate is added or weakened.
---

The decider carried, since 2026-09-14, that two fixture filers state dividends
under elements the registry does not name, so their dividend concepts fall to
the pair's conditional zero although the filers paid. The owner's record decides
it is a registry question and names the elements; this task writes the bytes.

The record also sets out what it leaves to the vocabulary's next version — a
declared-dividends concept and a witness condition on the pair's zero — and
none of that is here. This task edits two registry files and nothing else.

Runs beside anything: its two files are owned by no other task, and the
digest it changes is pinned nowhere.
