---
id: M4-50
title: Scale the benchmark proof's deliberate slowdown to the committed baseline
milestone: M4
owns:
  - scripts/gates.sh
  - benchmarks/normalize/baseline
  - crates/normalize/tests/bench.rs
depends_on: []
exclusive: yes
acceptance:
  - '`violate_benchmark` in `scripts/gates.sh` reads the benched stage''s
    committed `baseline` for `bytes` and `passes` and inserts, into the first
    public function `lib.rs` states on one line, found as today and not named,
    one allocation per call of `ceil(bytes / passes / 10)` bytes and at least
    one: `std::hint::black_box(vec![0u8; N]);` with `N` computed by the runner.
    As the accepted `docs/adr/benchmark-proof-scales-to-the-baseline.md` rules.
    A baseline missing either number is a proof that could not be made, and
    the runner says so and exits 3, as it does today for a signature it cannot
    find.'
  - '`benchmarks/normalize/baseline` gains one line, `passes 512`, with a
    sentence saying what it is. `allocations`, `bytes` and `cost` do not move.
    `crates/normalize/tests/bench.rs` reads the pass count from the baseline
    where it read the constant, and says so in its header; the count the
    measurement runs at is the count the file states.'
  - 'The full sweep, `scripts/gates.sh`, exits 0 on the tree as committed: every
    gate passes and every proof catches, the benchmark proof among them. The
    pull request states the injected `N` for the stub''s baseline and the
    figures the slowed copy measured.'
  - '`benchmarks/thresholds` is untouched. No gate is added, weakened or
    renamed; the gate list the runner expects does not change. Nothing under
    `crates/normalize/src/`, `fixtures/` or `contracts/` moves, and no
    dependency is added so the workspace lockfile does not move.'
  - 'Where `escalations/2026-09-25-M4-44.md` is on main when this runs, it is
    deleted in the same diff, this being the change that resolves it.'
---

M4-44 stopped because the proof of catch it is told to keep red cannot go red
once `normalize()` is the stage: one 21-byte allocation per call against
283,306. The owner's record scales the slowdown to the baseline; this task
writes it, on the stub, before M4-44 makes the stage do work.

Exclusive because it edits the gate runner, a protected path, and its pull
request needs the `human-approved` label, which the owner applies. M4-44 now
depends on this task.

Not in this diff: anything M4-44 does — `normalize()`, the fixture, the
re-recorded workload and baseline figures.
