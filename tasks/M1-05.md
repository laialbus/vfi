---
id: M1-05
title: Scope the style rules to their paths
milestone: M1
owns:
  - .claude/rules/
depends_on: [M1-01]
exclusive: no
acceptance:
  - .claude/rules/rust.md loads only for paths under crates/ (frontmatter
    paths key), and .claude/rules/python.md only for the shell's path as
    docs/layout.md names it.
  - git.md and comments.md remain unscoped (they apply everywhere), with
    their opening lines updated if they still claim scoping is pending.
  - Each scoped file's "not yet scoped" preamble is removed.
---

Mechanics: a `paths:` frontmatter key in each rule file, per Claude Code
memory documentation. The shell directory does not exist yet; scoping to the
layout-documented path now is correct — the rule simply will not load until
the path does.
