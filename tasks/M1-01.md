---
id: M1-01
title: Define and document the workspace layout
milestone: M1
owns:
  - docs/layout.md
  - README.md
depends_on: []
exclusive: no
acceptance:
  - docs/layout.md exists and maps every kind of artifact to exactly one home,
    covering the repo (crates, shell, scripts, tasks, sessions, escalations,
    docs, .claude, .github) and the workspace level around it (run.sh,
    prompts/, vfi-work/), including everything M0 created.
  - It states the crate naming convention the Rust workspace will follow.
  - It states the one-file-per-item rule for tasks/, sessions/, and
    escalations/, and why (parallel runs never collide writing records).
  - README.md points a newcomer at CLAUDE.md and docs/layout.md.
  - A new agent can tell where something belongs without asking.
---

The single source of truth for "where does this go". Every other M1 task
follows what this document fixes. See GOALS.md, M1.
