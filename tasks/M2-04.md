---
id: M2-04
title: Gate contract compatibility
milestone: M2
owns:
  - scripts/gates.sh
depends_on: [M2-09]
exclusive: yes
acceptance:
  - The gate checks the contracts for breaking changes against their versions,
    per anchor 3. The compatibility rule — what counts as breaking, what a
    version must do about it — is stated where the gate reads it.
  - With no contracts/ yet, the gate passes by running the real checker over
    an empty set and saying so. It is an absence, not a stub.
  - Proof-of-catch, fixture contracts carrying a breaking change without its
    version bump turn the single command red, running with the gates in CI.
  - contracts/ itself is not created. It is protected, and the first real
    contract is a later milestone's ADR-gated work; this task builds only the
    checker that will be waiting for it.
  - The gate runs from scripts/gates.sh and CI runs the same command.
---

The fixture contracts used by the proof live with the checker's own tests, not
in contracts/ — nothing in this task claims to be a real contract between
stages.
