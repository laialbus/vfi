---
id: M2-07
title: Gate the operational scripts
milestone: M2
owns:
  - scripts/gates.sh
depends_on: [M2-06, M1-07]
exclusive: yes
acceptance:
  - Every script under scripts/ is covered by the gate, at minimum
    syntax-checked under the shell it declares.
  - tasks.sh behaviour is pinned by a committed test corpus covering the
    classes the decider's PR #18 reviews proved by hand — malformed
    frontmatter refuses the listing at exit 3, exclusive gating in every
    claim state, dependency gating, the claim-then-verify flow, and the exit
    code distinctions. The throwaway harnesses become repository tests.
  - A change that breaks a pinned behaviour turns the single command red.
    Proof-of-catch runs with the gates in CI, like every other gate's.
  - The gate runs from scripts/gates.sh and CI runs the same command.
  - The corpus's home is a new kind of artifact; amend docs/layout.md in the
    same diff.
---

The decider's re-reviews of PR #18 flagged the gap directly: scripts/ has no
gate over it, so every behaviour verified in review is verified only in that
review. Depends on M1-07 so the corpus pins the finished claim behaviour, not
a moving target.
