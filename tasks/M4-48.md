---
id: M4-48
title: Record the golden fixture for the filer whose statements have no gross profit
milestone: M4
owns:
  - fixtures/normalize/a-filer-whose-shape-has-no-gross-profit/
depends_on: [M4-44]
exclusive: no
acceptance:
  - '`fixtures/normalize/a-filer-whose-shape-has-no-gross-profit` holds CIK
    0002033615 under the `bank` kind the registry holds for it, read from the
    merged fetch fixture of that name. It uses the input form, the claims form
    and the rendering M4-44 fixed, and adds nothing outside its own directory.'
  - '`claims` is hand-derived from that fixture''s facts and the published
    vocabulary, and each claim names what it comes from. It meets the floor
    `docs/adr/golden-claims-and-baseline-re-recording.md` sets for a filer no
    accepted record pins a claim for: the period set with its count and every
    line of one instant and one duration in full. It also holds the rows the
    bank kind excludes, which are the fixture''s reason to exist: every concept
    whose `applies_to` omits `bank` is `NotApplicable` at every admitted
    period, carrying the kind and the clause. v2 publishes six of them,
    `gross_profit` among them.'
  - '`expected` is recorded from the engine only once every claim holds
    against it, and the pull request says so. Where a claim does not
    re-derive, the run stops and escalates rather than restating it.'
  - 'Nothing proved gets narrower. Every other fixture still produces its
    expected result, and the crate, harness, registry, contracts and benchmark
    are untouched. No gate is added or weakened.'
---

One of the golden fixtures GOALS.md's M4 names, "a company whose statements do
not fit the ordinary shape". No accepted record pins a claim about this filer,
so the golden-claims record's floor is the whole of what the claims must hold.
It is the second filer of the `bank` kind, after M4-46's.
