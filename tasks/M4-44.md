---
id: M4-44
title: Make normalize() the stage, with the restatement filer as its first golden fixture
milestone: M4
owns:
  - crates/normalize/
  - fixtures/normalize/
  - benchmarks/normalize/
depends_on: [M4-38]
exclusive: yes
acceptance:
  - '`normalize()` is the stage: given one filer''s facts as the fetch
    boundary publishes them, it writes what M4-38''s surface answers for that
    filer under the committed registry, rendered with the content the accepted
    `docs/adr/what-normalize-emits.md` fixes under "What a normalize fixture''s
    `expected` renders". The filer comes first, then each admitted period by
    its dates in an order that reads only their characters. Each period holds
    all twenty-eight concepts in the order the vocabulary publishes them, and
    the way a value was set is rendered as a word. The registry version is one
    word where it is the fixture''s registry and sixty-four characters where it
    is not. This task fixes the spelling. It stays the first public function
    `lib.rs` states on one line, as the accepted
    `docs/adr/golden-claims-and-baseline-re-recording.md` requires, and the
    header of `lib.rs` says what it now does.'
  - 'The rendering spells every state and every way, including the ones the
    restatement filer does not reach: `NotApplicable` with the kind and the
    kinds its clause admits, an `asserted` value, and a tie. Hand-written cases
    in the crate pin each of them, so a later fixture task that reaches them
    edits nothing in the crate.'
  - '`fixtures/normalize/nothing-resolves-yet` is retired, and
    `fixtures/normalize/every-fact-a-filer-reported` holds CIK 0002003750.
    A normalize fixture is named for the merged fetch fixture whose facts it
    reads, as `suspension-fixture-is-the-bank-fixture.md` already names one.
    Its input is that fetch fixture''s facts for the filer, whole. Where the
    fixture holds a copy of them rather than reading them from
    `fixtures/fetch/`, something the gates run fails when the two part. A
    later fixture is added by adding its directory under `fixtures/normalize/`
    alone, with no edit to the harness or to any other file.'
  - 'Beside the input sit the two files the golden-claims record names.
    `claims` is hand-derived, and each claim names the record or the facts it
    comes from. It holds at least every claim the accepted records make about
    CIK 0002003750 as they now stand. That means the counts
    `silence-zero-only-at-the-concepts-own-shape.md` re-states: 32 periods, 19
    durations and 13 instants, 51 silence values, and 576 `Unknown`s. It means
    the Rule 3 tables at the half-year 2023-10-01 to 2024-03-31 and the quarter
    2024-01-01 to 2024-03-31, with `short_term_investments` `Unknown` there. And
    it means `earnings_per_share_diluted` at −0.0015 and −0.0004, naming
    `0001213900-25-042964`. It also holds every line of one instant and one
    duration in full. `expected` is the full rendering, recorded from the
    engine only once every claim holds against it, and the pull request says
    so. Where a claim does not re-derive, the run stops and escalates rather
    than restating it.'
  - 'The golden harness runs every fixture through `normalize()` and checks
    `expected` byte for byte, as it does today. The claims check the
    golden-claims record adds to the harness is not in this diff: it is an
    exclusive task with its own record. Until it lands, the decider
    re-derives the claims by hand.'
  - 'The benchmark workload and baseline are re-recorded together in this
    diff, as the golden-claims record states. The workload is one filer''s
    facts as the fetch boundary publishes them, taken from a merged fetch
    fixture. The baseline is taken over it at the pass count the harness
    states. The old and new figures are stated in the pull request and the
    session entry, and `benchmarks/thresholds` is untouched. The benchmark
    proof of catch still goes red on the deliberate slowdown. Where it can no
    longer catch it, the run stops and escalates rather than shipping a gate
    with no proof.'
  - 'Nothing proved gets narrower. Every existing crate test holds, including
    every count and claim M4-38 and M4-43 pin. The registry is not edited, no
    contract byte changes, and no dependency is added, so the workspace
    lockfile does not move. No gate is weakened, and the golden, benchmark and
    every other proof of catch in `scripts/gates.sh` still catches.'
---

The step M4-38 names as not in its diff: "`normalize()` and the golden
harness moving onto this, and the normalize golden fixtures, which fix the
rendering the record leaves to them." The 2026-09-18 planner stopped before it
on two questions, and `golden-claims-and-baseline-re-recording.md` answers
both. It asks for a claims file beside a recorded `expected`, and for the
workload and baseline to be re-recorded in the same diff. The first fixture
has to land in this diff because the stub's fixture stops holding the moment
`normalize()` does work, and the harness refuses an empty sweep. The
restatement filer comes first because M4-38 already pins the most claims
about it.

Exclusive because it moves what two gates check, the golden fixtures and the
benchmark baseline, even though neither gate's script changes.

Not in this diff: the other four filers' fixtures, each its own task after
this one, and the harness's claims check. This task reads a fetch fixture and
calls no fetch function. Whether the task that composes the fetch calls end
to end passes `company_facts` the history the funnel already holds, noted by
the 2026-09-19 lead, stays with that task.
