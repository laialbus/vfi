---
id: M4-46
title: Record the golden fixture for the filer that suspended its dividend
milestone: M4
owns:
  - fixtures/normalize/a-filer-that-suspended-its-dividend/
depends_on: [M4-44]
exclusive: no
acceptance:
  - '`fixtures/normalize/a-filer-that-suspended-its-dividend` holds CIK
    0001778784 under the `bank` kind the registry holds for it, read from the
    merged fetch fixture of that name. It uses the input form, the claims form
    and the rendering M4-44 fixed, and adds nothing outside its own directory.'
  - '`claims` is hand-derived, and each claim names the record or the facts it
    comes from. It states the period set with its count, which
    `docs/adr/period-alignment.md` asks the recorder of this fixture to say,
    and every line of one instant and one duration in full.'
  - 'It holds the dividend claims of the Enforcement section of
    `docs/adr/declared-cash-dividends-stand-in.md` for this filer. Under the
    ruling listed in `docs/adr/README.md`, the silence-zero claim means the
    year 2024 and every 2025 period the fixture carries.'
  - 'As `docs/adr/suspension-fixture-is-the-bank-fixture.md` decides, every
    concept whose `applies_to` omits `bank` is `NotApplicable` at every
    admitted period, carrying the kind and the clause. The emit record''s other
    claims about this filer are re-derived under that kind, including the year
    2023''s five attempts for `revenue`.'
  - '`expected` is recorded from the engine only once every claim holds
    against it, and the pull request says so. Where a claim does not
    re-derive, the run stops and escalates rather than restating it.'
  - 'Nothing proved gets narrower. Every other fixture still produces its
    expected result, and the crate, harness, registry, contracts and benchmark
    are untouched. No gate is added or weakened.'
---

One of the golden fixtures GOALS.md's M4 names, a dividend suspension. It is
also the first fixture to reach a `NotApplicable`, as
`suspension-fixture-is-the-bank-fixture.md` decides. That record queued it
after M4-42, now merged, and takes its dividend claims from the declared-cash
record.

The emit record's `short_term_investments` claims on this filer are
superseded by the bank record and are not pinned.
