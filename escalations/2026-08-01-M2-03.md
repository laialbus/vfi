# Escalation: M2-03

- Date: 2026-08-01
- Branch: M2-03
- Commit: 38ff08c

The purity gate that lands with this task is a dependency deny-list, which is
the mechanism anchor 4's enforcement clause names. It cannot see std.
`std::fs::read`, `std::net::TcpStream`, `SystemTime::now` and `std::env::var`
need no dependency at all, so once analyze has source, it can reach the outside
world without tripping anything. Analyze is empty today, so nothing is
unchecked yet; the first task that writes analyze code changes that.

Not blocking: the task is delivered and every gate passes. What is above a
worker is the decision, not the code. Widening how an anchor is enforced is a
constitutional call, and adding an unasked-for second gate to the protected
runner is outside this task's boundary.

To resolve: decide whether anchor 4 wants a second check over analyze's own
source — the shape would be a source lint, not a dependency list — and queue a
task for it if so. If the answer is that the deny-list is the whole of the
enforcement anchor 4 asks for, deleting this file is the answer.
