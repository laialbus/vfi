# Escalation: M4-10

- Date: 2026-09-02
- Branch: M4-10

The registry gate walks the concept edges the `difference` form draws and
refuses a cycle. It walks the general half only. A filer file may now include a
`difference` rule of its own, so a filer's eligible set — the general rules,
less what its file excludes, plus what it includes — can hold a cycle no gate
sees.

Not blocking: the task is delivered, every refusal it names is proved, and
nothing in the tree is unchecked today. `registry/filers/` holds no file, and no
code composes an eligible set yet — this task implements no reader and no
interface, by its own boundary.

What is above a worker is where the check belongs. Walking the composed set
means composing it inside the gate, which is the work
`docs/adr/tag-concept-registry.md` gives to the one interface both halves are
reached through; a second copy of that in bash is the unchecked duplication the
one-source-of-truth invariant bans. The record's enforcement list does not name
this check and this task's acceptance does not either, so adding it was not this
run's call.

To resolve: decide whether the acyclicity of a filer's eligible set is the
gate's or the interface's, and put it in M4-11, which is where the set is first
composed. If the answer is that the general half's acyclicity is the whole of
what data alone can check, deleting this file is the answer.
