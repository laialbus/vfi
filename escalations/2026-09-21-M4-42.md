# M4-42 — one Enforcement claim does not re-derive: no filing answers the year 2025 on CIK 0001778784

**Doing.** I wrote the three `stand_in` entries as
`docs/adr/declared-cash-dividends-stand-in.md` states them. Then I re-derived
the record's Enforcement claims with a throwaway test through
`standing::stands`. It took each filer's kind from `Registry::kind_of` and ran
the new registry against a copy of the committed one. I have reverted the
entries, so nothing is committed but this file.

**What stopped it.** The record says that on CIK 0001778784 "the years 2024 and
2025 remain the silence zero for both". For 2025 that does not hold. No filing
in `fixtures/fetch/a-filer-that-suspended-its-dividend` answers
2025-01-01..2025-12-31. The last filing is the 10-Q `0001437749-25-033757`,
through 2025-09-30. So `stands` returns `None` for both dividend concepts, under
both registries: no attempt runs, and there is no value of any kind. The task
says to stop in this case rather than adjust the claim.

**The readings that re-derive, not taken.** Every 2025 period the fixture does
carry reads the silence zero for both concepts, and it does so under both
registries. Those periods are 2025-01-01..03-31, ..06-30 and ..09-30,
2025-04-01..06-30 and 2025-07-01..09-30. Each rests on a `Dividends` 0 and none
on the new entries. So the record probably means "the 2025 periods". It may
instead mean the filer's 2025 facts, or the claim may simply be wrong for this
fixture. Choosing between those is the record's call, not a worker's.

**Everything else re-derives, with the entries in place.**
- CIK 0001778784:
  - Q2 2020: `dividends_paid` 583000 from `DividendsCommonStockCash` in
    `0001778784-21-000046`, beside a read 0.03 (it was `Unknown`).
  - 2022: 1989000 from `DividendsCommonStockCash` in `0001778784-24-000006`
    (it was `PaymentsOfDividends` in `-23-000010`).
  - 2023: 0 from `DividendsCash` in `-24-000006`, beside an `Unknown` per-share
    figure.
  - 2024 and Q4 2020: the silence zero for both, as before.
- CIK 0001859199:
  - 2025: `preferred_dividends` 122877 from `DividendsPreferredStock` in
    `0001213900-26-026653`.
  - `dividends_paid` is unchanged at all 97 periods.
- CIK 0002003750: the fixture carries no fact under any of the four elements.
  All 28 concepts at all 50 periods are unchanged.
- The registry version moves from `329d9764…887b` to `f48d8c61…1d9e`.

**Needed to proceed.** A ruling, in the record or the task, on what the 2025
claim for CIK 0001778784 asserts. If it means the 2025 periods the fixture
carries, the task can proceed as written. The entries and header sentences are
ready.
